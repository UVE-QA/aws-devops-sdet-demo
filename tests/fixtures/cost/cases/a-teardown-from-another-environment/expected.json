{
  "description": "The prod teardown handed to the stage apply. The two runs are each internally fine, which is exactly why nothing downstream would have noticed.",
  "refused": "the destroy timeline is prod, not stage"
}
