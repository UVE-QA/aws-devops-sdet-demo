# Session Index

One row per session. Read this first; open a specific summary only when needed.

| Date | Phase | Summary | File |
|---|---|---|---|
| 2026-06-06 | 0 | project scaffold created (pre-devbox) | sessions/2026-06-06-project-scaffold.md |
| 2026-07-25 | 8 | planning: ADR-0017, MVP-first next-phases, control layer committed | sessions/2026-07-25-planning-and-control-layer.md |
| 2026-07-25 | 9.0 | prod scaffold reconciled; ECR to a permanent level (ADR-0018); validation gate closed; Project mirror retired (ADR-0019) | sessions/2026-07-25-phase-9-0-reconcile-prod.md |
| 2026-07-26 | 9.1 / 11.0 | OIDC provider split from the deploy role (ADR-0021); prod trusts no branch; repository published so the approval gate can exist (ADR-0022, ADR-0023); state exchange moved to clone-in/patch-out (ADR-0020) | sessions/2026-07-26-phase-9-1-oidc-split.md |
| 2026-07-26 | 9.1 CLOSED | prod deployed and destroyed through Actions; promotion by digest behind required reviewers; HTTPS on a delegated subdomain (ADR-0024); teardown check scoped per environment; two IAM reads found only by running | sessions/2026-07-26-phase-9-1-prod-promotion-https.md |
| 2026-07-26 | 10 | items slice (create/list/delete + negatives); read-only and destructive suites split by directory (ADR-0025) after prod turned out to run the whole testDir; UI-write assertion proves the browser reached RDS; one shared ecs-run-task helper. Code complete, nothing run against a database or AWS | sessions/2026-07-26-phase-10-thin-application-slice.md |
| 2026-07-26 | 10 CLOSED | full cycle green through Actions (deploy-stage, promotion behind reviewers, read-only smoke against prod, destroy both); teardown verified against the AWS CLI; `--use-device-code` was documented once and contradicted in eight files; a credential-less teardown check reads as a clean account; the teardown skill still expected ECR to disappear; prod looked dead for 30 min on a client-side negative DNS cache | sessions/2026-07-26-phase-10-aws-validation.md |
| 2026-07-26 | 11.1a | gitleaks debt from 11.0 paid over the full history and the scan proven able to fail; ADR-0026 splits dashboard status by what each source can observe; ADR-0027 makes the dashboard a permanent state level; infra/public-site scaffolded, nothing applied; docs/security-posture.md answers what a public repository does and does not expose | sessions/2026-07-26-phase-11-1a-decisions-and-scaffold.md |
| 2026-07-26 | 11.1b | infra/public-site APPLIED (16 added, 0 destroyed, first attempt): private bucket + CloudFront/OAC + us-east-1 certificate + apex/www + narrow publish role, verified against the CLI as 403-with-valid-TLS on an empty bucket; both data sources resolving at plan time proved the two prerequisite levels applied; the new level's lockfile was untracked and one hash short; publish plumbing written across two roles with one status file per environment; publish-site green first attempt under the narrow publish role with 200 asserted by the workflow, and the sync guard that protects status/ and reports/ proven in both directions with a canary and a --dryrun; the Owner tag had diverged (uve vs UVE) and the preflight doc named a third value nothing carried; status steps in the three lifecycle workflows still unrun until 11.1c; the teardown skill now lists five permanent levels | sessions/2026-07-26-phase-11-1b-apply-and-publish.md |
| 2026-07-26 | 11.1c | dashboard content written: environment panels from `status/<env>.json`, per-STEP status of the current cycle from the anonymous Actions API, twelve runs of history, and the permanent-vs-per-cycle architecture; ADR-0026's staleness rule scoped so `ci` cannot make a panel amber and a `destroy` run can be attributed at all (`run-name: destroy <env>`, one line of YAML that makes the run observable); every degradation explicit — banner on 403, "no observation" instead of "destroyed", `unverified` when freshness cannot be checked; six fixtures through a stub DOM because a healthy cycle shows none of those states. NOTHING RUN: no status file has ever been written | sessions/2026-07-26-phase-11-1c-dashboard-content.md |
| 2026-07-26 | 11.1c CLOSED | full cycle under a live dashboard, no manual AWS operation: deploy-stage #21, promote-prod #4 and destroy prod #12 both behind a required reviewer, destroy stage #13; the 11.1b status plumbing ran for the first time and was green in all four; stage and prod were WATCHED going no observation → up → unknown → destroyed, with the run named at each step; promotion by digest and `run-name` both became legible on one screen. Three defects, all in the browser half and all the same shape - a panel claiming "nothing has reported" while a deploy reported, an absence rendered as a failed read at the approval gate, and a refresh cadence indistinguishable from a dead page. The AWS side, where failure was predicted for the fourth time, did not fail once; and a `grep ... || echo none` written this session printed `none` for a missing directory | sessions/2026-07-26-phase-11-1c-dashboard-content.md |
| 2026-07-27 | 12 | minimum viable documentation: the first README this repository has ever had (absent from the whole history, not stale), architecture with Mermaid diagrams that were PARSED rather than eyeballed, and a ten-minute demo script with its traps inside it; project-prompt §7/§10 rewritten from `git ls-files` and ADR-0015/0021; the tf-workflow skill stopped teaching a validation command that reads remote state, and the fix went to every copyable occurrence; control-layer tooling brought into git with the bare-name bug fixed (ADR-0028); `make docs-check` added to ci.yml, seen red six times before it shipped, and it found a false claim that had been in the cursor since Phase 6. No cycle run - a deliberate, recorded exception, since the phase changed no infrastructure | sessions/2026-07-27-phase-12-documentation.md |
| 2026-07-28 | 13 CLOSED | MVP verification gate: one empty-to-empty run, all six steps. Promotion by digest checked against ECR rather than against the dashboard that asserts it; teardown verified from the devbox before AND after, so the prod destroy was a fact and not a self-report. `destroy` on prod turns out to stop at the approval gate too and the demo script said not to wait for it; self-approval is permitted and is now stated rather than found; `pending_deployments` needs `-F`. The UP badge is a snapshot in the present tense, one-directional and unfixed. The AWS half did not fail once; the chat half asserted a 209-line document did not exist and `git am` refused to overwrite it | sessions/2026-07-28-phase-13-mvp-verification.md |
| 2026-07-28 | 14 CLOSED | release resilience. The phase's own premise was wrong and had to be corrected first: "roll back to the previous task definition" is meaningless where prod is destroyed every cycle and the previous revision is deregistered, so the target became a digest pointer at a permanent level (ADR-0029) - the fourth independent arrival at the ADR-0027 rule. Registry made IMMUTABLE and `:latest` dropped, which nothing had ever read; a re-run of deploy-stage now reuses its image instead of colliding with its own tag. Rollback OBSERVED firing: a knowingly broken image was promoted, the service never stabilised, prod was re-applied from the pointer and the smoke was RE-RUN green while the run stayed red. Both no-target refusals exercised, one live and one against a digest that had genuinely just been deleted. Three defects found by running rather than reading - the rollback trigger would have rolled back a HEALTHY release when the bookkeeping after a green smoke failed; SSM reserves parameter names starting with `aws` and this project is named that; a runner has no git identity so `git tag -a` exits 128. Teardown verified empty from the devbox after being non-empty from the same commands | sessions/2026-07-28-phase-14-release-resilience.md |
| 2026-07-28 | 15a | supply-chain gates, part one, at $0: Dependabot over five manifests, gitleaks as a gate in ci.yml, and the budget email moved from a GitHub variable to an environment secret. Dependabot's first run raised SIX stale actions where the annotation channel had been showing two - the four silent ones include configure-aws-credentials, two majors behind, which authenticates every AWS workflow. Its PR is deliberately unmerged: it touches five workflows and `ci` runs one. The secret gate's break test FAILED TO BREAK - a planted AWS access key id scanned green, and four probes showed why: in gitleaks 8.30 an AKIA identifier alone is not a finding, the pair is caught by the entropy rule rather than the AWS one, and the 11.1a instruction to assert on the AWS rule is therefore impossible. gitleaks was not installed on the devbox at all, two days after it paid the Phase 11.0 debt | sessions/2026-07-28-phase-15a-supply-chain-gates.md |
| 2026-07-28 | 15b | scanning gates, at $0: Checkov over infra/ with 4 findings fixed and 46 skip decisions written beside the checks, Trivy over the built image gated on what is FIXABLE and reporting what is not, and 32 actions pinned by commit SHA (ADR-0030) with a check that keeps them pinned. Trivy went red then green in CI on a real vulnerability rather than a planted one - three starlette HIGHs, and Dependabot's PR #5 turned out to be the fix, verified by resolving fastapi 0.140.13 in a clean venv rather than by reading a version range. The red was chosen over the tidy merge order, because a CI job only ever seen green is indistinguishable from one that cannot fail. Four of the 46 Checkov skips are the scanner failing to evaluate a `dynamic` block, not a posture gap. A gate on a shared dependency reddened all four other open PRs through no fault of their own; checkov exits 0 on a directory with no Terraform; the devbox had no Checkov installed, exactly like gitleaks two days earlier; and a break test measured with a pipe measured the pipe | sessions/2026-07-28-phase-15b-scanning-gates.md |
| 2026-07-29 | 16a | contract depth: GET by id, PATCH with exclude_unset, a paginated envelope and updated_at (ADR-0031), an inline edit control, and a second UI probe that is RENAMED through the browser so the database assertion can require an UPDATE rather than an insert with the right name. Four patches, three of them because something ran: a test that skipped itself on its first run and had its fixture BUILT instead; a deliberate off-by-one that passed all 50 contract tests, because every pagination assertion was about the newest rows and an off-by-one drops the oldest; and two tests that timed out against the stage ALB after passing on localhost, because `data-loaded` is a one-way flag and a spec waiting on it was answered by the render from before its own click. Full cycle green, both environments destroyed and verified with a positive control in the same command; the 15b action-version debt ran for the first time and did not fail | sessions/2026-07-29-phase-16a-contract-depth.md |
| 2026-07-31 | 16b CLOSED | structured logs and the 5xx alarm. The two halves are one decision (ADR-0032): the metric filter reads the log, so the shape of the log decides whether the alarm can exist - status as a NUMBER, one line per request including the ones that RAISE, and a request id the caller can choose. A new in-process suite holds both properties, because no HTTP client can see either. Four break tests fired, one of them unplanned: `make docs-check` refused a README change because tests/unit was not yet tracked. Three findings, all from running - uvicorn's ANSI-coloured `color_message` riding into CloudWatch through `extra=`; `default_value = 0` making the metric billable from the first ALB health check while the ADR claimed it did not exist until the first 5xx; and the alarm holding ALARM for exactly sixty seconds, notifying nobody, recorded only in describe-alarm-history. The AWS half of the break test used the LINE the local fault produced, one field changed | sessions/2026-07-31-phase-16b-structured-logs-and-5xx-alarm.md |
| 2026-08-01 | Ops | session entry and exit become commands (ADR-0033). Phase 16b closed four times, each time reported complete, and the exit checklist was never missing - it exists in four documents, three of which were read. So: `make session-open` refuses on a dirty tree, an unpushed previous session or the wrong branch and prints the phase from the CURSOR; `make session-close` checks the summary, INDEX in both directions AND in order, the narrative's date, a clean pushed tree, and prints the Consequences of any ADR added. Two findings from running them: session-open reported the WRONG session, because the row closing 16b had been inserted above the 16a row and the table was no longer chronological; and the ordering fix was then discarded by `git checkout` during its own break test, which the primer has warned about since 2026-07-28. The session also started by building a documentation linter instead of what was asked, and discarded it | sessions/2026-08-01-ops-session-entry-and-exit-as-commands.md |
| 2026-08-02 | 18 CLOSED | remaining documentation, pulled forward of 17 and 19 for the same reason 12 was pulled forward of 13: no infrastructure changes, and both open phases draw on it. `docs/cost-control.md` (permanent-vs-per-cycle split, the two cycle costs already measured closing 16a and 16b - $0.09, $0.17 - cited not re-derived, the budget alarm's real thresholds), `docs/interview-talking-points.md` (five roles, every point traced to an ADR or a session summary, Phases 17-19 explicitly marked planned not built), `docs/lightsail-devbox.md` (the devbox's role versus the AWS deploy target, the SSH tunnel, the two non-default login flags needed since Phase 1). No ADR - no structural decision. None of the three joins the LIVING set `docs-check` enforces, so every ADR number, `make` target and path they cite was checked by hand instead - 12 ADR references, 2 make targets, all repository paths, zero findings. No cycle run, the Phase 12 exception again | sessions/2026-08-02-phase-18-documentation.md |
| 2026-08-02 | 19.0 | self-service launch, decisions and plan; no code, nothing applied, no AWS API called. ADR-0034 (trigger path) and ADR-0035 (guardrails), decided ahead of 19a because the expensive mistake in this phase is a design that cannot refuse. The button REVERSES this project's one direction of trust - everywhere else GitHub authenticates to AWS over OIDC, here AWS must authenticate to GitHub, where no OIDC exists - so "no static keys anywhere" gains a qualifier: no static AWS keys, and exactly one static GitHub credential in Secrets Manager readable by one Lambda role. The lock, the day counter and the kill switch are the SIXTH arrival at the ADR-0027 rule. The public path cannot reach prod by IAM rather than by an input, and the nonce is labelled a speed bump rather than authorization, because the goal is not that only the right people can press it but that it does not matter who does. Numbers rather than adjectives: TTL 90 min, 3 launches/day, ~$0.30/day worst case against the measured $0.09 and $0.17 cycles; the cap FAILS CLOSED, an unreadable counter is not zero launches today. One finding, and it amends the plan: the out-of-band watchdog cannot be a cron on the devbox - a cron has no human and the devbox reaches AWS through a typed device code, so unattended means a static key on disk, and the domain actually distrusted is Actions rather than AWS. EventBridge plus a Lambda instead | sessions/2026-08-02-phase-19-0-decisions-and-plan.md |
| 2026-08-02 | 19a | self-service scaffold, written and NOT APPLIED, $0: the sixth permanent level (control store, launch Lambda behind a Function URL, EventBridge watchdog, kill-switch Lambda on an SNS topic, and a callback role that can delete exactly one item), the launch workflow with its `if: always()` destroy and lock release, 21 in-process assertions over every refusal, and the dashboard button behind a flag. The finding is a guardrail that would have eaten its owner: ADR-0035's "a missing deadline is expired, not exempt" deletes the OWNER's stage environment, which carries no deadline because no deadline is what "a human is watching this one" looks like - fixed by a Launch tag that is empty for an owner cycle, enforced in the handler AND as an IAM condition, where the Null test is not redundant with the StringNotEquals because an absent tag makes StringNotEquals TRUE. The plan also did not know the Lambda runtime ships no crypto: an RS256 JWT needs PyJWT and cryptography vendored, so `make self-service-package` exists and refuses three ways. Four break tests fired locally; not one of the five refusals has been proven against the real table, which is 19b. Validated on the devbox, and running it corrected the chat twice, both times about a TOOL rather than the code: `terraform fmt` does not align across a multi-line value, so the sandbox's approximate fmt checker measured an assumption about the tool rather than the tool; and Checkov found CKV_AWS_297, a tenth skip decision the list had not predicted from reading the resources. tf-validate 8 levels, test-unit 28, iac-scan 290/0/56, docs-check 0 findings, action-pins 43, ci green in all four jobs | sessions/2026-08-02-phase-19a-scaffold.md |
| 2026-08-05 | 19b CLOSED | the self-service level APPLIED (25 resources, ~$0.45/month permanent) and every refusal 19b owns fired: `kill_switch` from a message shaped like the one AWS Budgets really sends, `store_unavailable` from both halves naming `issue_nonce` and `get_flag(killswitch)`, `locked` naming its holder with no run queued (positive control in the same command), `daily_cap` with the lock proven released afterwards. Two defects found by applying, both green under tf-validate, iac-scan and 21 in-process assertions the day before: the concurrency reservations are impossible while the account's Lambda quota is 10, and a public function URL has needed TWO policy statements since October 2025 - the provider writes one, so the missing half is the half nobody wrote, and the fix took provider ~> 6.0 on this level alone (zero drift). Two dead ends recorded: an organization policy closed on `AvailablePolicyTypes`, which AWS documents as unreliable, then properly by `list-roots`; and a throwaway function that reproduced the defect and looked like proof the account was at fault. A third finding came from USING a guardrail - the kill switch has no way back anywhere in the repository, and its message names the budget alarm whatever the cause. Checkov's CKV_AWS_301 fired for the first time because the fix made the public grant visible at all, and became the repository's first inline skip. Endpoint left PARKED behind the kill switch | sessions/2026-08-05-phase-19b-apply-and-refusals.md |
| 2026-08-05 | 19c | the button pressed for real: wired to the endpoint 19b applied and left pointing at an empty string, then an anonymous browser press reached a dispatch and a full cycle deployed, smoke-tested and destroyed itself with the dashboard reporting it live. TTL 90 min read out of the lock the code writes; ExpiresAt and Launch tags on the resources, so the deadline outlives both the lock and Actions; `locked` proven against a REAL lock, which also showed `run_url` is read in one place and written nowhere. Three defects found by running: two access-control-allow-origin headers on every reply, which only a browser can see because the CORS layer needs an Origin and preflight never reaches the function - both morning probes were built so they could not see it, and `make self-service-cors-check` now fails on zero as loudly as on two; all three repository variables `release-lock` reads missing, so that job had never worked, and a missing vars.X is an empty string rather than an error; and the page itself. Break test 3 confirmed always() runs after cancellation - and revealed the state that keeps 19c OPEN: a cancelled apply leaves an S3 state lock plus resources that never reached state, destroy dies on the lock, release-lock releases anyway, and dispatch_destroy skips its record when the lock is gone, so the blunt path cannot engage in the one case it exists for. Seen in the log re-dispatching every five minutes while an ALB and RDS billed. Break test 5 forced and passed against a real environment, AWS CLI as witness; recovery took force-unlock and three orphans deleted by hand after "re-run destroy" burned two fifteen-minute timeouts. Account verified empty, endpoint parked | sessions/2026-08-05-phase-19c-live-launch-and-teardown.md |
| 2026-08-05 | 19d | the state a cancelled run leaves, decided rather than patched (ADR-0036). Three defects, each green in isolation, combining into one from which the only exit was a human: the watchdog wrote its dispatch record onto the LOCK and skipped the write when there was no lock - so it forgot every dispatch in exactly the case it exists for, and the blunt path could not engage; `release-lock` deleted the lock without asking how destroy went, which also reopened the button onto an apply guaranteed to fail; and the documented recovery, "re-run destroy", cannot work because re-running destroy takes the state lock that is stuck. Fixed as three parts: the watchdog gets its own scoped, ttl-bearing item and its decision moves to a pure `sweep.py` that tests can drive branch by branch; release-lock releases only on `needs.destroy.result == success`; and `scripts/break-stale-state-lock.sh` breaks a lock whose holder is this job's own runner user when no other run is in progress, refusing in every other case - `Who` as evidence rather than age as a guess. Batched: the kill switch reports the source it was thrown with instead of always naming the budget alarm, and refuses GET as well as POST as the README always claimed; `run_url` removed from the `locked` refusal because it is read in one place and written in none; the dashboard adopts ttl and cap from the endpoint's reply. Three findings from running - a new assertion that modelled a cancellation as already past its deadline and so measured its author, the preflight aborting on an unbound variable outside Actions, and a break test measured through a pipe reporting exit=0 over a command-not-found. 41 unit assertions, five preflight refusals fired on fixtures, nothing applied | sessions/2026-08-05-phase-19d-cancelled-run-recovery.md |
| 2026-08-06 | 19e | the break test, and the claim it narrowed. First: publish-site was dispatch-only, so the public page ran 20h behind main - now triggered by push on site/** with concurrency, exception recorded in security-posture, proven by a real push. Then a launch cancelled mid-apply on purpose confirmed all three ADR-0036 decisions on LIVE evidence: the preflight broke the stale state lock in 6s, release-lock kept the lock on destroy=failure, and the watchdog wrote its own scoped item and dispatched destroy, taking the RDS instance and the ALB with no human. What it disproved is the wider claim: the remainder took FOUR manual AWS calls. Root cause named at last - a security group reference chain (rds-sg <- app-sg <- alb-sg) makes deleting alb-sg impossible while app-sg lives, so -target=module.alb retried an impossible delete for 15 minutes and the main destroy and its verification never ran. The identical 15m40s failures of 2026-08-05 were read then as a consequence of the lock. Fixed by targeting module.alb.aws_lb.this (f1ed545); ADR-0037 records that as D1 and leaves D2-D4 to 19f. Account verified empty, destroy green end to end | sessions/2026-08-06-phase-19e-break-test-and-teardown-gap.md, evidence in sessions/2026-08-06-phase-19e-watch.log |
| 2026-08-07 | 19f | ADR-0037 D2-D4 shipped and all three confirmed live, and the claim they were written for disproved: a cancelled launch still needed three manual AWS calls. D2 revoked the security-group chain against real groups and the group that held destroy for 15m22s yesterday left without comment; D3 made Verify and the new sweep RUN on a job that had already failed, where the identical situation on 2026-08-06 produced a run reporting success while an ECS cluster survived. The gap is now exact and is an ORDERING: resources created by a cancelled apply never enter state, Terraform can neither delete them nor delete what depends on them, and the blunt path clears the billable ones only AFTER the dispatched destroy has failed - the destroy that would then succeed is never run. Three findings from running: D3 as the ADR wrote it would have turned a skipped step into a GREEN one, because always() lets it run after a failed credentials step and every aws call then answers nothing; the tagging API was wrong in both directions within one hour, missing an RDS instance that was still creating and reporting a security group a minute after AWS deleted it - the stale direction would have reddened every teardown and, through ADR-0036 D2, kept the public button shut until its TTL after every launch; and an exclusion removed on the theory that a more general mechanism subsumed it turned 22 deregistered task definitions into `unconfirmed` and the gate red on an empty account. One defect caught by reading before it shipped: a bare call under `set -e` where non-zero is the ordinary answer | sessions/2026-08-07-phase-19f-teardown-sees-what-it-leaves.md, evidence in sessions/2026-08-07-phase-19f-watch.log |
| 2026-08-07 | 19g | ADR-0038: a teardown ADOPTS what it does not manage before it destroys, chosen over re-dispatch and widen by reading them against the code - re-dispatch re-runs a destroy that still does not manage the cluster and the security groups, because those are free and the blunt path only deletes what bills; widen puts Terraform's dependency graph inside a Lambda. Adoption works one layer earlier and the ordering dissolves: the watchdog's existing dispatch has always been the retry, and was ineffective only because the destroy it dispatched could not adopt. Its input is the 19f gate itself, so the two cannot disagree about what an orphan is. Two cancelled launches, three defects, each found by the previous fix WORKING. The one underneath: `confirm_exists` read a resource's kind as everything up to the first SLASH, and AWS separates kind from name with a slash OR a colon - so rds:db, rds:subgrp, logs:log-group and secretsmanager:secret were four case arms that had never once been reached, and every resource of those kinds answered `unconfirmed`. Adoption reads `orphans` and not `unconfirmed`, correctly, so the RDS instance was never offered to it. It also corrects ADR-0037's diagnosis, which blamed the missed instance on its `creating` status - it was never reportable at all. Invisible where it was tested, because none of those kinds is tagged once an environment is gone, including on a read-only run against the empty account ninety minutes earlier: a gate is only exercised by the case it was built for. With that fixed, the second launch adopted 4 of 4 INCLUDING module.rds.aws_db_instance.this, and the destroy died on a null endpoint address - an instance three minutes old has none, Terraform evaluates config during a destroy too, and the state was unreachable before adoption existed. The fix moved the failure rather than removing it. Third: cloudwatch:alarm and elasticloadbalancing:listener had no existence rule and turned `unconfirmed` in the first sweep this project has run against a LIVE environment. Both destroys that finished were green with the account verified empty and a positive control in the same command; both were dispatched by hand to skip a 90-minute wait, so `nobody in the loop` is still a prediction and the phase stays open. My own sequencing error cost the first launch: the patch was on the devbox and not on main, and the workflow comes from main | sessions/2026-08-07-phase-19g-the-teardown-reaches-the-end.md, evidence in sessions/2026-08-07-phase-19g-watch.log |
| 2026-08-08 | 19g CLOSED | the uninterrupted run, and with it Phase 19. A launch cancelled mid-apply at 00:44:15 with the fullest orphan set any cancel here has produced - ALB active, cluster up, three security groups, RDS `creating` - and the run's OWN destroy job started nine seconds later, swept (`verdict: orphans`, exit 1, five of them), adopted 4 of 4 including `module.rds.aws_db_instance.this` at two minutes old, named the fifth UNADOPTABLE rather than dropping it, and finished GREEN at 00:49:09. 4m45s, zero manual AWS calls, no watchdog, no blunt path, nobody dispatching anything; `release-lock` then released, which by ADR-0036 D2 happens only on destroy=success, so a second job agrees independently. That sentence had closed four consecutive summaries as a prediction. The mechanism was read rather than assumed, and each of 2026-08-07's three defects met its own case for the first time in that single run: `rds:db` reported at all (the colon/slash parser), an instance adopted while still `creating` no longer killing the destroy on a null address, and ZERO `unconfirmed` lines where the live sweep printed two the day before. Two findings arrived before the button was pressed, both about documents rather than infrastructure: the endpoint was NOT parked - two files said the kill switch had been thrown by hand after 19c and the control store held no such item, so the public button had been live since 19g cleared it, and it now STAYS live as the recorded finished state of Phase 19; and a day's three launches belong to the UTC DAY rather than to a session, which is why the counter read 3 where a summary described 2 - 19f's break test and 19g's two launches shared one cap. The first explanation offered for that discrepancy was wrong and checking it took one command, the same shape ADR-0037 got wrong about the `creating` instance a day earlier. Shipped: `scripts/watch-launch.sh`, the watch loop as a checked-in script - it had been typed into a terminal on three separate days and broke the same two ways every time, neither of them a property of the loop - with no field allowed to be blank, because `alb=none` is what a torn-down environment and an expired token look like alike. Break-tested offline on all three branches before it recorded anything. Account verified empty from outside the run with a positive control in the same command; about $0.03 | sessions/2026-08-08-phase-19g-nobody-in-the-loop.md, evidence in sessions/2026-08-08-phase-19g-watch.log |
| 2026-08-08 | 20.0 | a planning session that found its own subject: ELEVEN places across eight files described the applied, publicly-pressed, closed `infra/self-service` as written-and-never-applied - README (contradicting itself: line 14 knew it was applied, line 148 said it never had been), architecture's prose AND its Mermaid node, cost-control's "if the button is ever public (NOT BUILT)" and "none of this is running", interview-talking-points' "planned, not built", and the LIVE dashboard telling every visitor there were five permanent state levels while standing on the sixth. A sixth fell out on the way past - architecture's "seven state levels" heading, correct while there were five permanent ones. No gate could see any of it: `make docs-check` verifies that every path, target, route and workflow a document NAMES exists, never that what it CLAIMS is true, and two of the five files are not even in its document list. Third arrival of the species in three days and the largest - 19g found two documents disagreeing with the control store, this found five disagreeing with the account, in the files an outsider opens, on a public repository, for six days. The answer taken is NOT a bigger linter (a check for "not applied" catches this instance and nothing else; the next stale claim will be a number or a tense) but ADR-0039 D1: the architecture section is GENERATED from infra/ behind a drift gate, and prose survives only as a second rendering of the same file. Phase 20 planned as 20a map + gate ($0), 20b timeline from terraform's own -json event stream, 20c tests panel generated from the suites, 20d cost computed and reconciled against one real bill. D4 was corrected by the user mid-session and the correction is better: the map is PERMANENT, not a panel that appears during a cycle - at rest it carries the last measured cycle with its date, which is what a visitor almost always sees. Two consequences the plan did not have: an identifier is not an ARN (`id_value` is the provider's id - an ARN for aws_lb, an instance identifier for aws_db_instance), and per-resource live pulsing would need the deploy role writing the bucket ADR-0026 keeps it away from, so live is per PHASE from the Actions API and the two ways to buy more are priced and untaken. A cost figure was invented and caught in review: $0.09 and $0.17 are 16a's and 16b's cycles, and 19c has no recorded figure at all - a gap in the record, not a zero. Asked afterwards whether ALL the documents were fixed, a second sweep found SIX more that the first had missed entirely - and not one of them a phrase. They were COUNTS: `architecture.md`'s "Three levels are permanent for that one reason" above a list of four, `demo-script.md`'s "Seven root levels: five permanent" and "twenty-seven ADRs" against thirty-nine, the tf-workflow skill's "seven root levels" with a list missing self-service, and - the expensive one - `docs/session-primer.md` itself listing seven levels without the sixth permanent one, wrong since 19b in the file every session reads FIRST. The first sweep had grepped for the wording it already found; a count goes stale without a word changing around it and reads as precision while it does. Reported as fine before that, on the strength of the Mac primer copy MATCHING the repository - synchronised and correct are different questions, and only the first had been checked. Consequences taken rather than noted: ADR-0039 D1 now requires topology.json to carry the counts with prose rendering them, and the primer's working agreements gain the rule the day earned - never rush, never wait to be asked to check, verify before reporting. The label rule gained its second dated example from the same session, when two unlabelled blocks in one reply sent a [mac] scp to the devbox. Eleven stale places, $0 | sessions/2026-08-08-phase-20-0-generated-not-policed.md |
| 2026-08-08 | 20a | the layout, built and LOOKED AT before the generator, because it is the only part of Phase 20 that cannot be derived from the repository. Rendered in a sandbox clone against a hand-built fixture at 1440/1180/834/390. The finding is that a PICTURE makes claims: folding the chain node by node drew VPC -> Secrets Manager -> RDS -> ALB as a sequence, and Terraform creates those from a DAG - nothing in infra/ orders them. ADR-0026's rule, written about JSON documents, applies unchanged to a diagram, which this project had not had to notice. Fixed by moving the unit of sequence up: the PHASE carries order, the nodes inside it are a set with no arrows, and phases fold serpentine. The same fix cured a legibility problem nobody had raised - in the node fold one row read across three phases and the phase survived only as a digit in a chip. Mobile came free exactly as ADR-0039 D5 predicted: at one column the serpentine has nothing to fold, same code, no media query. Desktop and laptop declared the intended view IN the page rather than engineered around, decided with the user. Icons: ADR-0039 forbade guessing, so three AWS pages were read and the answer is that AWS DOES NOT SAY - the icons page permits "architecture diagrams" and lists materials "like whitepapers, presentations, data sheets, and posters" with no licence document and no mention of a website, the trademark guidelines give no blanket permission to display marks publicly and forbid altering them, the site terms forbid reproduction "for any commercial purpose without express written consent". What was taken is a DECISION in the absence of a no, not a permission, and assets/aws-icons/NOTICE.md keeps those apart; 17 unmodified icons in one inline sprite so the page stays one file with no request-time dependency, and a test suite, a human approval and a teardown keep PROJECT glyphs because they are not AWS services. Two tool errors caught by measuring twice: a raw `chrome --headless --screenshot --window-size=390` showed a clipped phone layout that does not exist (old headless does not set the layout viewport from the flag; through Playwright scrollWidth == clientWidth at all four widths) - the primer's pipe rule, in a browser; and `make docs-check` went red on this session's own phase-gates entry for naming `make site-data-check`, a target 20a has not built - a planned target written in the present tense in copyable form, by the session writing about not doing that. The fixture is where the schema came from, on purpose: the layout decided what topology.json must carry, so the generator inherits a contract instead of inventing a shape. Sprite builder's two refusals exercised, both red. NOT done and not claimed: the generator, the site-data-check drift gate and its break tests, and folding the map into site/index.html, which was deliberately untouched. $0, nothing applied, no AWS API called | sessions/2026-08-08-phase-20a-layout-pilot.md |
| 2026-08-08 | 20a generator | the fixture retired: site/data/topology.json is GENERATED from infra/ and tests/ by scripts/generate-topology.py, and `make site-data-check` runs in ci.yml as a COVERAGE gate - every resource block under infra/ in exactly one display group, including the group meaning deliberately not shown - plus a drift check that the committed file is byte-identical to a fresh generation. It REFUSED on its first run against unmodified infra/ and was right: nine resource blocks carry count or for_each and the hand-built fixture counted each as one, so "116 resources" was never the number of objects AWS creates - three of them are `count = 2` - two subnet sets and the route-table associations - drawn as one each. The repair is a change of UNIT, not a better guess: every count says resource BLOCKS, each of the nine is acknowledged by name with its reason, and a tenth appearing without an entry is red. The same check then reported the ALB security group and the HTTP listener as repeated, matching `for_each` four spaces inside a `dynamic "ingress"` block - Phase 19g's exact finding, in a different file, in a session that had read it that morning; anchoring on indentation is a claim about formatting and the question is about depth. Replaced with a brace walk. Five break tests instead of the two planned, all red, exit codes written to a FILE and the tree committed before anything was broken: an unassigned resource block, the generated file deleted, a count edited by hand ("adrs" 40 -> 27, the number demo-script.md carried that morning), an assigned resource deleted from a module, and a node removed from a phase whose module is still instantiated. A sixth path is green AND RECORDED: the deliberately-not-shown group, holding aws_default_security_group, which creates nothing - the module's own comment says AWS makes it with the VPC and Terraform adopts it - and break test 5 is what proves that skip is not blanket. The generator writes NO observation at all - duration, cost, identifier and result are things a cycle says - so the map renders unobserved until 20b/20c, a visible regression in the exhibit and the honest one. Rendering it found a defect no check here could name: the CSS class defaulted a missing `state` to absent while the body branched on the raw field, so every node took the MEASURED body and printed `undefineds`; nothing in the JSON was wrong and only a screenshot said so. Playwright at real viewports 1440/1180/834/390, scrollWidth == clientWidth at all four. Then two things on review: the map had NO devbox and NO GitHub, because neither is in infra/ and the map is generated from infra/ - and neither is a phase, since nothing creates or destroys them, so they became a band ABOVE the cycle, with the GitHub card's claim about the trust path TIED to aws_iam_openid_connect_provider.github through a `backed_by` field that reddens the gate if the provider is deleted; Lightsail's icon was then added from the same package release and the same `48` set as the others so it shares their viewBox - the `64` set was taken first and would have rendered at a different weight, caught by listing every file's viewBox rather than trusting the filename. GitHub keeps a glyph for a DIFFERENT reason, and the difference is the point: its brand page NAMES this case and permits it ("inform others that your project integrates with GitHub", "less prominently than your own product name") where AWS's three pages neither permit nor exclude a public web page - but the mark may not be modified and a redrawn Octocat is a modified one, so the asset must be downloaded; assets/github-logo/NOTICE.md records the terms so nobody assumes the AWS reasoning transfers. Adding that one icon made FOUR written numbers false at once - "seventeen" in the sprite builder twice, in the template and in the plan, and "~48 KB" for a sprite now 50,168 bytes - one of them inside assets/aws-icons/NOTICE.md, the file whose job is to explain why the icons are safe to use. Sixth time in two days that the stale thing was a number rather than a phrase; all four replaced by measurement or by no number at all. And the front page's prose was asked to survive under a cut - it does, GENERATED from the same file, since keeping it as prose preserves exactly the class of defect the phase exists to end; the generator emits `members`, the terraform address behind every node, so the cut names all 116 blocks the map folds into 26 marks. Looking again found three things and the third is older than the session: a terraform address is one unbroken token and pushed the phone 103px past its viewport with the cut OPEN while the same page with the cut SHUT fitted and said nothing (and the first wrap fix covered `dd` only); opening the cut summoned a scrollbar and the map had been laid out against the wider viewport; and `.node .head` is up to 22px wider than its own node at every width, on the PRE-PATCH build measured by stashing this one - the pilot's `scrollWidth == clientWidth` was true of the DOCUMENT, while the head spills into the node's own padding where neither a screenshot nor that measure could see it, left unfixed on purpose because layout here is decided by looking. site/index.html deliberately untouched, the last piece of 20a. $0, nothing applied | sessions/2026-08-08-phase-20a-generator-and-drift-gate.md, evidence in sessions/2026-08-08-phase-20a-break-tests.log |
| 2026-08-08 | 20a CLOSED | the map is on the front page, and the page is a build output. The hand-written "What happens, in the order it happens" section - seven chips and the lane that said five permanent levels while standing on the sixth - is replaced by site/data/topology.json rendered as the band, the permanent levels, the serpentine and the cut. Because the map needs the icon sprite inline, site/index.html stopped being hand-written: assets/index.template.html is the source, `make site-page` builds it, and `make site-page-check` in ci.yml requires the committed page to be byte-identical to a fresh build - a committed build output invites being edited in place, and that edit survives exactly until the next build silently reverts it. The pilot is retired, page and template and target, and publish-site syncs with --delete so the published copy went with the commit; two renderings of one file maintained separately is the shape this phase removes. Two layout defects, both invisible to the pilot's own green check because it measured the DOCUMENT: the serpentine packer charged ONE gap per join where a row renders phase|gap|arrow|gap|phase, so at 1180 the first row was 1132px inside a 1125px box, and `.node .head` was up to 22px wider than its node - recorded and left by the generator session because layout here is decided by looking, and this is the session that looked. The head took three attempts and the failures are the useful part: overflow-wrap:anywhere stopped the overflow and rendered "Applicati on Load Balancer", flex-wrap:wrap stopped the breaks and put the icon on a line away from its own name; what was stealing the width was the environment tag, redundant with a phase header that already says stage or prod, kept because 20c gives a suite an identity of suite x environment. Six break paths on the new gate, five red, both controls green, exit codes to a FILE with the tree committed first - including the drift case the gate exists for, the template edited and the page not rebuilt. One claim in that log was written before it was checked and the first check ran against a CLEAN tree and returned 0, proving nothing. Three more stale numbers on the way past: "17 objects" over a list of eighteen in the sentence explaining why a number should not be written down, a character count printed as bytes (118,577 against 118,667, which is what em dashes cost), and "no build step" in three documents. Closed with two fixes to the session machinery: session-close printed the wrong summary when a day holds more than one, because `ls | tail -1` sorts alphabetically - both orderings now come from INDEX's last row, the one this script already refuses to let go non-chronological - and the primer's WORKING chat name came from the phase title, which does not change while a sub-phase spans sessions, so 20a's three would all have opened under one name; it comes from the cursor's next allowed step now. $0, nothing applied | sessions/2026-08-08-phase-20a-the-map-on-the-page.md, evidence in sessions/2026-08-08-phase-20a-page-break-tests.log |
| 2026-08-08 | 20b.1 | the $0 half of the timeline: terraform's -json stream is captured by scripts/tf-stream.sh in all eight apply/destroy invocations across the four workflows, folded by scripts/fold-timeline.py into timeline/<env>/<run id>-<job>.json AND back into a legible per-resource log, and gated by `make timeline-check` in ci.yml. Three signals decide completeness and the weakest wins - the .rc written after terraform returns, the exit code, and the terminal change_summary - because an apply killed mid-flight emits a change_summary saying "plan", which a fold reading only the stream would report as a plan rather than as a half-finished apply. The fixtures are REAL terraform runs and apply-killed was really killed; apply-complete-no-rc was added because apply-killed is over-determined and cannot show which rule is doing the work. Two things the plan did not have, both silent if missed: the publish key needs the JOB as well as the run id, because self-service launches and destroys inside one run; and publish-site.sh's --delete would have removed every published timeline at the next push to main. Seven break tests red, both controls green, tree committed first. The OpenTofu caveat is discharged: authored against 1.10.6, regenerated on the devbox with terraform 1.15.8, all six expectations unchanged - proven not by the identical event counts but by terraform.ui / ui 1.3 replacing tofu.ui / ui 1.2 across 99 lines. Recorded rather than fixed: the devbox runs 1.15.8, the workflows pin 1.15.5. Nothing applied, no AWS API called, nothing on the page | sessions/2026-08-08-phase-20b-1-a-killed-apply-is-not-a-cycle.md, evidence in sessions/2026-08-08-phase-20b-1-timeline-break-tests.log |
| 2026-08-08 | 20b.2 | the consumer half, at $0, and writing it changed the producer: 20b.1's latest.json was described as "the map's at-rest source" and cannot be one - it is overwritten by every run whatever happened, so ONE cancelled run would erase a dated complete measurement and the page would show the difference as absence. ADR-0040 splits it: latest.json says what happened LAST including that it did not finish, nodes-apply.json / nodes-destroy.json carry the NUMBERS and are published only when a cycle completes, and the page draws the second while saying the first - which also gives 20b.2's live break test somewhere to be SEEN rather than fetched. The join from a timeline onto the map's nodes is scripts/node-states.py on the RUNNER, not a few lines on the page: written there it would be JavaScript on the page and Python in its own gate, the shape that cost this project a scan of the wrong image from a byte-identical Makefile recipe. `make node-states-check` in ci.yml carries ADR-0039 D1's coverage rule applied to observations - a resource a cycle created is drawn, is recorded as deliberately not drawn, or is named UNKNOWN, never silently absent - over 4 cases folded from real terraform runs plus one hand-written case that says so, because terraform_data finishes in zero seconds and no real run can tell "the longest member" from "the first member". The stub topology carries a prod node holding stage's member addresses on purpose: stage and prod are the same modules in two state levels, so dropping the environment filter lights half the map from the other environment's cycle, and that goes red. The module question 20b.1 left open is answered OFFLINE instead of during the billable run - a local module holding terraform_data is as real a terraform run as the others and costs the same nothing, so cases/apply-module names three address shapes including count on the MODULE, which infra/ does not have today; its expectation is a prediction and generate.sh on the devbox turns it into a measurement or a red gate. Smaller, each late otherwise: MAXDUR, the constant scaling every bar, was 261 - measured once by hand and written into the page ADR-0039 made generated - now computed; the live pulse binds a phase to a workflow STEP because every workflow here has one job, and generate-topology.py refuses a binding naming a step or job that does not exist (made red both ways); the map does not call the Actions API at all but receives the dashboard's reading as a DOM event, because 60 anonymous requests an hour is the whole budget. THEN THE CYCLE RAN, and the apply failed on a defect this session did not write: EntityAlreadyExists on module.ecs.aws_iam_role.task and .execution, both in AWS and in no state - orphans that survived because the teardown verifies itself by asking whether any BILLABLE resource remains and an IAM role is free, so a green check stood over a not-empty environment (written up in next-phases, unfixed, third teardown leftover after ADR-0037 and ADR-0038 and the first that cost nothing and broke everything). The failure handed over the live break test for free, which is stronger than staging one: latest.json published as errored, nodes-apply.json 403, the page saying the most recent run did not finish - ADR-0040 D1 on a real event. The teardown then ran green, 261 seconds over 26 resources, nodes-destroy.json complete and drawn. And the coverage gate spoke about something no fixture contained: module.network.data.aws_availability_zones.available, action read - terraform emits apply_start/apply_complete for DATA SOURCES too, a data block is not a resource block and can never be assigned to a display group, so leaving it in `unknown` would be a permanent false positive in the one channel that is supposed to be rare; it has its own bucket and a fixture now (terraform_remote_state over a local state file, offline and free). Reading that file also caught observed.unknown counting EVENTS while the list beside it named unique addresses. Two defects visible only while something ran: the Destroy phase lit BOTH environments while one was being torn down, fixed by taking the environments from the dashboard's own run-name parse rather than re-deriving them, and a phase that says 'running now' and nothing else cannot be told from a page that has stopped updating, so the header now carries elapsed time. CLOSED with one criterion openly unmet: the apply half was never lit, nodes-apply.json has never existed, and the map's service nodes are still unobserved - the evidence cannot be obtained now (another apply fails identically on the same orphans) and costs nothing later (20c needs a cycle regardless), while the path itself was driven end to end by the teardown. The cursor therefore points at the teardown finding rather than at 20c: it blocks every deploy-stage until a gate exists that can see a free leftover. Under $0.05 The module question 20b.1 left open is now MEASURED, not predicted: run on the devbox against terraform 1.15.8 the expectation held to the character - addr is fully module-qualified and carries the module's own index as well as the resource's (module.pair[0].terraform_data.many[0]), hook.resource.module is redundant with it, and the six existing fixtures regenerated to identical expectations and identical event counts. Six break tests red, both controls green, tree committed first: the two that matter are the environment filter removed from the node index - stage and prod hold IDENTICAL member addresses, so the stub topology carries a prod node holding stage's members precisely so that reddens - and a hidden group that forgets which addresses it hides, where the coverage claim is that an unclaimed address becomes UNKNOWN rather than quietly not-shown. Then two findings in the session machinery itself: `make session-open` had been naming the wrong phase for a day, because ADR-0033's cursor read "the last data row of the status table" and that is the FURTHEST-OUT phase - since 20.0 added three planned rows at once it announced 20d, blocked on 20b, to every session including this one - while reading the status column instead fails in the other direction, the first not-done row being phase 8, parked since July; it now reads the last `- Next allowed step:` line and the heading above it, the line the document already writes for this purpose. And the first fix pointed at a DIFFERENT wrong row: /^#{2,3} / is an interval quantifier and under the devbox's mawk 1.3.4 it matched `## Completion criteria & validation` and none of the thirty-three `### Phase` headings - caught by running it rather than by reading it. ci.yml green on the push with the new step confirmed to have EXECUTED, not merely the run green. | sessions/2026-08-08-phase-20b-2-a-cancelled-run-erases-nothing.md, evidence in sessions/2026-08-08-phase-20b-2-break-tests.log |
| 2026-08-08 | Ops | the gate that sees a free leftover, and the roles it found: two IAM roles left by a green teardown on 2026-08-05 blocked every deploy-stage for three days with EntityAlreadyExists. Nothing could see them - the teardown's gate asks about BILLABLE resources and a role is free; a partial teardown had dropped them out of state; and the tagging API does not index iam:role, so the sweep was never handed one. Its fail-closed confirmation was blameless. The tagging API DOES return iam:oidc-provider from the same state level with the same tags in the same call, which killed the wrong-region explanation with a control inside the same answer. ADR-0041: for kinds nothing indexes, discovery comes from the CONFIGURATION, because a collision can only happen on a name the configuration will create - a prefix scan was approved and then found unrunnable, the deploy role having iam:GetRole on two ARNs and neither ListRoles nor ListRoleTags. One control per channel now: a shared one would have the loud channel vouch for the silent one. Patch 1 adopted the role alone and would have been WORSE than adopting nothing - DeleteRole refuses while a policy is attached, so the import hands destroy a DeleteConflict: red and still leaking. Caught by asking AWS what was attached before removing anything; the teardown had leaked four objects, not two. Not pushed until patch 2 mapped the dependents. Break test never planted: the same command said clean/exit 0 at 17:50 and orphans/exit 1 at 19:20 with only the code changed, then destroy.yml adopted 4 of 4 and the verdict came from aws iam get-role, not from the green workflow. Also: CI red on site-data-check for the new ADR, and its message named the wrong directories; a gh run list -L 1 measured publish-site instead of ci; chat session links switched off in .claude/settings.json, unproven until a devbox Claude Code commit | sessions/2026-08-08-ops-the-gate-that-sees-a-free-leftover.md, evidence in sessions/2026-08-08-ops-live-break-test.log |
| 2026-08-08 | 20c | the suites answer for themselves, plus one live cycle: the map's suite nodes stop carrying a sentence written beside them and a count of FILES, and start carrying an inventory COLLECTED by the tools that run them, plus a fold from the reporters' own output. Reading the repository first killed three lines of the plan - tests/db has no collector (it was GIVEN --list), the map's gate runs in a job with no test dependencies, and the db assertion exists TWICE with the cloud running the copy the local gate does not. Eleven deliberate defects with a green control either side; one refused to break as designed and that is written down. A decision reversed before pushing by `gh pr list`: the collected version left the artifact because the open Dependabot PR bumping @playwright/test would have gone red saying nothing (ADR-0042 D3). THEN THE CYCLE: apply 31276975666 lit the apply half 20b.2 never reached, real results published - api 52, regression 12, smoke 2 - and db came back incomplete for a suite that had passed, because `aws logs --output text` joins an array with TABS and three events arrived as one line. A race was assumed and a fix written before the log was read; one command refuted it. Teardown confirmed against the AWS CLI, account empty | sessions/2026-08-08-phase-20c-the-suites-answer-for-themselves.md, evidence in sessions/2026-08-08-phase-20c-the-suites-answer-for-themselves.log |
| 2026-08-08 | 20c CLOSED | a node answers for its own step: the page read neither the collected inventory nor the published reports, so everything the previous session established was true and invisible. It reads both, and the three findings from watching that cycle turned out to be one structural mistake - liveness was attached to the BOX a node is drawn in. A suite node now carries its own `live` binding, checked against the workflow exactly like a phase's; a node without one inherits and is MARKED as having inherited, so "its phase is running" replaces seven nodes claiming to be running while Terraform created one; and a phase that finished mid-run says so instead of showing the sentence a phase that never ran shows. ADR-0043. The state machine is the one fold that cannot move to Python - it reads the Actions API in the browser - so the gate lifts the marked block out of the BUILT page and runs it against twelve recorded observations. Ten break tests, including all three findings put back; the sharpest line is suite.db.stage reported as nothing while its step ran. One more defect found writing the gate: the phase clock restarted at every bound step, so a four-minute apply read as ten seconds old. $0, no AWS call. Evidence: sessions/2026-08-08-phase-20c-a-node-answers-for-its-own-step.log | sessions/2026-08-08-phase-20c-a-node-answers-for-its-own-step.md |
| 2026-08-08 | Ops | the site sync deleted the results it was publishing a reader for: publish-site.sh runs `aws s3 sync site/ --delete` and excluded status/, reports/ and timeline/. ADR-0042 had added a FOURTH thing the lifecycle writes - results/ - hours earlier, and nothing added the line. It stayed harmless because the two scripts only meet on a push to main touching site/, and the push that finally did was 20c's own page release: the commit teaching the map to read those results deleted them. No versioning, so cycle 31276975666's folded results are gone; the timeline, node states and reports survive, being excluded. Established by a control differing in one variable - same script, same run, one prefix excluded and present, one not and empty - after a 403, an empty listing and a silent `gh run view --log` had each decided nothing. Fixed in one line, and the rule is now a check: ADR-0044, `make publish-prefixes-check`. Six break tests, the first of them the unplanted defect itself. Two documented traps walked into while writing them: a `git checkout --` discarded the uncommitted fix, and the --delete guard was a substring test its own comment made unfireable. Evidence: sessions/2026-08-08-ops-the-site-sync-deleted-the-results.log | sessions/2026-08-08-ops-the-site-sync-deleted-the-results.md |
| 2026-08-08 | 20d | cost is a lifetime, not a creation: a cycle's cost is now computed by a command from its own timelines and a captured rate table, instead of being worked out by hand in a session. The obvious number - terraform's `elapsed_seconds` - is the wrong one: it is how long the resource took to BUILD, and the meter runs for as long as it EXISTS. On the cycle of 2026-08-08 the load balancer took 173 seconds to create and then stood for 1582 more, so an estimate on the creation figures would have reported a ninth of its cost and looked reasonable. It reports a BAND, because terraform cannot see the instant AWS starts charging: RDS in that cycle was between 852 and 1381 seconds of meter, 62% apart. Prices are captured from the Price List API with their SKUs, shapes are read out of infra/ so nothing records them, and which kinds are worth metering is editorial and says so. `make rates-check` reads the kinds out of the configuration: a NAT gateway added to the network module reddens a gate rather than costing money invisibly. The cycle came out at $0.018-0.024, and five sixths of it accrued outside every phase - the environment simply up. Thirteen break tests; one crashed instead of asserting and was re-aimed, and the coverage gate's green control could not exist until the table was captured. ADR-0039 D3's promised reconciliation against a real bill is RETIRED (ADR-0045 D6), not deferred. **ADR-0045**. Evidence: sessions/2026-08-08-phase-20d-cost-is-a-lifetime.log | sessions/2026-08-08-phase-20d-cost-is-a-lifetime.md |
| 2026-08-08 | 20f | the teardown prices the cycle it just ended, and the plan it was blocked behind turned out to describe a decision nobody had taken: 20e's line about moving the per-node prose into hover was a spontaneous example, written into docs/next-phases.md with a costed collision worked out beneath it, in the one document a session reads to find out what to do next - the usual stale document says something that WAS true, this one said something that never was. 20e is restated from the requirement as given (the dashboard is a long strip you cannot navigate - wayfinding, not density), opens with a discovery step of its own, and moves AFTER the wiring; three findings recorded so it does not rediscover them, including that the body markup is 3.0% of site/index.html so "compact" cannot mean fewer bytes. Then the wiring ADR-0045 left undone: ADR-0046's pairing rule, four clauses with the fourth deliberately weak because ADR-0038 adopts orphans before a teardown, so only a pair with NOTHING in common is refused - orphan_deletes was already computed and nothing refused on it. The anchor is timeline/<env>/apply.json written inside the block that publishes nodes-apply.json, under the same kind and completeness, so the anchor and the numbers beside each node cannot disagree; fetched over anonymous HTTPS because the object is public and the cheapest new path is the one that is not new. Pricing is continue-on-error on purpose: a red teardown holds the launch lock. THE BREAK TEST BROKE AND IT WAS THE INSTRUMENT - four different defects reddened one fixture, then the identical loop reported all four green, because CPython validates a .pyc on (mtime in whole seconds, source size) and every one-clause break leaves fold-cost.py at exactly 18911 bytes inside one second; settled by measuring the five variants rather than reasoning about them, and all five importlib-loading gates fixed in one commit. The re-run then found clause #1 untested - written, shipped and unexercised. make publish-prefixes-check went red unprompted on cost/, the first new prefix since ADR-0044, naming the remedy: the 2026-08-08 deletion prevented rather than repeated. End to end on the real cycle: $0.018339 .. $0.023797, what 20d computed by hand. 11/11, no cycle, $0 | sessions/2026-08-08-phase-20f-the-cost-fold-runs-in-the-cycle.md, evidence in sessions/2026-08-08-phase-20f-the-cost-fold-runs-in-the-cycle.log |
| 2026-08-09 | 20e.0 | the dashboard is composed, not scrolled - and the discovery renamed its own phase. "Navigable" was the target the previous session recorded and it is close enough to be dangerous: a section index makes a long strip traversable, which is not the same as it not being a long strip. The requirement as actually given is a DASHBOARD rather than a long list of resources - where things are, how they connect, WHICH TOOLS are used, in what order, composed in blocks, detail under cuts - with the desktop monitor named as the primary target and the phone last. The complaint was measured rather than quoted, at four viewports with three remote sources unreachable so every figure UNDERSTATES the live page: 3.6 screens at 1920x1080, 10.5 at 390x844, and 0 in-page anchors, 0 <nav>, 0 sticky elements - the page has no navigation affordance at all, which makes the complaint literal. The per-cycle map is 46% of the page on a laptop; the repository link, the one thing a thirty-second visitor needs, is in the footer at 100% of scroll depth. Then a channel nobody had measured: five of the map's six states ride a 1px border and nothing else, and three are under WCAG 1.4.11's 3:1 floor in the light theme (working 2.67, done 2.41, suite 1.98) - solid versus dashed at one pixel is not a channel, which is what was reported by eye - while the pulse fades a 1px ring to opacity 0, so it is legible for a fraction of each beat and absent for the rest. THE FIRST CONTRAST MEASUREMENT WAS WRONG AND LOOKED LIKE A FINDING: color-mix() resolves to color(srgb 0.44 0.62 0.90), the parser divided those by 255, and six different colours came back at 20.92:1 in light and 1.23:1 in dark. Settled by a control inside the measurement - black on white must read 21:1, through both notations - which is printed on every run now. The tools turned out to be missing rather than their icons: Terraform, Docker, Playwright, pytest and Alembic appear NOWHERE on the map and TEST does not distinguish Playwright from pytest; named in text, with vendor marks left as separate work exactly as assets/github-logo/NOTICE.md already did for GitHub's. The sketch is built from the real topology and suite data with placeholder run figures and says so on its own face: 1.1 screens at 1920x1080, 1.0 at 2560x1440, and 0 boxes overflowing their own parent at any viewport - measured per-parent rather than against the document, 20a's lesson, which immediately caught a table 74px past its container that the document measure could not have seen. auto-fit left phase 8 alone on a second row with a screen of air beside it, so the column count is deterministic and its span total must be COMPUTED rather than written down. A phase whose nodes all say the same thing says it once in its header - ADR-0043 D4's disclosure stated, not printed twenty-six times. Collapse follows GitHub Actions: the header answers whether anything is wrong and names the FAILING step, the lines inside carry per-step status - the first version collapsed to "the current step", which for a finished run is the last one, a green tick where the thing broke. States now carry a 4px edge AND a word (4.48-5.18 light, 7.18-9.40 dark), with `not run yet` left under the floor ON PURPOSE and written down so it is not "fixed". Six things left open and named, including that the contrast floor has no gate yet. $0, nothing applied, site/ and assets/ untouched | Closing found one more, in the closing machinery itself: session-close's narrative check required '**As of <date>.**' exactly, every Current state block written since 2026-08-08 carries the phase in parentheses, so the expression fell through the file to line 514 - an entry from an earlier phase - and read ITS date. It had been GREEN BY COINCIDENCE: that stale line says 2026-08-08 and so did the newest session on the day four sessions closed under it. It spoke on the first day the two numbers differed, and neither of its verdicts was about the file it claimed to read. Demonstrated on the pushed file, one clause changed, old reading line 514 and new reading line 9; a first attempt rewrote the entry as well and made both expressions agree, which proved nothing and was reverted. The document convention is left alone - reformatting a dozen blocks to suit a reader fixes the wrong half. | sessions/2026-08-09-phase-20e-discovery-and-sketch.md, sketch in sessions/2026-08-09-phase-20e-sketch.html, evidence in sessions/2026-08-09-phase-20e-session-close-break-test.log |
| 2026-08-09 | 20e.1 | the floor is met BEFORE the layout, and the three items that blocked space are decided. The Launch button becomes the Environments panel's footer - it acts on what that panel observes and its refusals ARE environment state, while the identity bar is navigation and a control that spends money does not belong in a row of links, and the current-cycle panel's content varies so a control there would move. The legend becomes a cut in the map's header, closed by default, because ADR-0047 D6 put a word on every node and a decoder is no longer what a legend is - and explicitly the sketch's "State encoding" strip does NOT go on the page, being evidence for a decision rather than an element of a dashboard. A node's figures live on one state line, <word> then <figure>, word first because the word is D6's second channel and the colour gate cannot check it, with no separator printed when there is no figure - "passed · " with nothing after the dot is the empty result that looks clean. Then the gate ADR-0047's own Consequences asked for: `make contrast-check` lifts the <style> blocks out of the BUILT page, the same move check-live-state.mjs makes on the same file, and measures them in chromium, because the engine is what resolves color-mix() and the one contrast measurement this project has already taken was wrong IN A COLOUR PARSER. A control runs first on every invocation - black on white must read 21.00 through the hex notation and through the srgb-float one - and a control that is off REFUSES with no verdict rather than printing a table under a broken instrument. IT REPRODUCED ADR-0047'S TABLE TO THE HUNDREDTH ON FIVE OF SIX STATES, in both themes, written from scratch and sharing no code with the discovery's script, which was never committed; the sixth is a finding, `absent` reading 1.15/1.12 against 1.34/1.35, and the older figure is not a different model but an unreachable one, since #d8dbe2 against pure white is 1.27 - superseded by ADR-0048 D4, with nothing following from it because absent is exempt on purpose and was under the floor on both readings. The palette moved first and on the CURRENT page: working 2.67->3.22, done 2.41->3.37, suite 1.98->3.38 in the light theme, percentages measured as the smallest 5% step clearing the floor with a margin in both themes, and every boundary is now a token in :root so .node.done and .phase.done share one definition instead of two matching literals. The fix landed in its own commit BEFORE the gate, because a gate arriving red on a shared dependency reddens every open pull request over findings none of them introduced, which the image scan already did once. Broken seven ways with a green control either side and exit statuses read from $? rather than through a pipe: a state put back under the floor, --ok lightened with no state colour edited (done falls 3.37 -> 1.68, which is how the three got under the floor in the first place), THE DISCOVERY'S OWN PARSER DEFECT REPLANTED VERBATIM (refused on the control at 1.01, not 21.00, before a single state printed), an empty contract, no contract at all, a probe that is not a colour, and the built page missing. An eighth refusal fired unplanned during development, on playwright resolving from a CommonJS package with no synthesised named export - a fallback to parsing the CSS there would have been a control that reproduces the defect. ADR-0047 D5's computed span total was DEFERRED to the layout commit rather than done: it is computable today and nothing would read it, and a number nothing reads has never been exercised. $0, nothing applied, the composition not started | The transfer buffer's outbox/ held seven patches at session start, all twelve of their commits already in origin/main - the second session in a row to report it, since a chat cannot delete from the buffer. The MacBook's copy of the primer was verified byte-identical to the repository's by hash rather than by assertion. | sessions/2026-08-09-phase-20e-1-the-floor-is-met-before-the-layout.md, evidence in sessions/2026-08-09-phase-20e-1-contrast-gate-break-tests.log |
| 2026-08-09 | 20e.1 | the composition on the real page: ADR-0047 D1-D6 and ADR-0048 implemented in assets/index.template.html. The first screen is four blocks - identity with the links, environments with the Launch button as its footer, the request path with the TOOL on every hop, the run in flight collapsed to three lines - and the map below them is ONE GRID whose column count is computed in the generator, one row of eight phases at 1920 and above. Everything else is under five cuts, each answering its own question in its header. Measured with the same fixtures either side: 4781px -> 1935px at 1920x1080, 4.4 screens -> 1.8, and 1.3 at 2560x1440 which is the stated primary target. ADR-0049 records the six things building it settled that the sketch could not - one page width, the map folding into whole ROWS rather than taking the columns that fit (nine of ten stranded phase 8, the exact picture D5 exists to end), the legibility floor being a property of the node SHAPE, a state's edge being a background COLOUR and never a gradient, the environment tag being said once like a shared state, and the request path generated from citations that the generator refuses to leave dangling. Five break tests, controls either side: the gradient edge - which is what the sketch drew - reads 1.00:1 and the gate refuses, so copying the sketch verbatim would have shipped a state nobody was measuring. | sessions/2026-08-09-phase-20e-1-the-page-is-the-dashboard.md, evidence in sessions/2026-08-09-phase-20e-1-composition-break-tests.log |
| 2026-08-09 | 20e | the figures get an instrument. The cursor's step was the phone and its own line said to measure first; the layout is not started, and what the session established is that the measurement now outlives it. Every figure this phase argues from - 10.5 screens, 4.2, 5.8, "52px wider than a 390px viewport" - came out of a harness written inside a session and thrown away with it, so `scripts/measure-page.mjs` and `make measure-page` are committed. Not a gate: no verdict, nothing in ci.yml depends on it. It measures height, every box that sticks out of its PARENT'S padding edge, and every box whose content is wider than itself - the second because 20a measured the document's scrollWidth at four viewports and was green at all four while a phase row was 7px wider than the box it was packed into. The three remote sources are frozen in tests/fixtures/page-measure/ (at-rest, with both status files captured live, and in-flight, the tallest honest shape), because a page that could not read them is SHORTER than the real one rather than visibly broken; an unmocked request or a source-failure banner refuses the run, and meta.now pins the clock so the ages do not drift. Four break tests, controls either side, CONTROL 1 identical to CONTROL 0 line for line. An accidental replication came with it: different harness, different fixtures, heights within a tenth of a screen of 20e.1's, so the baseline is checked rather than inherited - and cuts OPEN, never measured before, is 19.6-20.2 screens on the phone. Two findings. The phone's only box that overflows its parent is the history table, and its "52px" is not a constant but a property of what ran recently: 84.6px without a self-service launch in the history, 169.1px with one, 174.6px in flight, split by an unbreakable ss- token and by the word "environment" in a header. And one that is not the phone at all: the text glyphs are wider than their badge on EVERY viewport including 2560 - WWW by 11px, TEST by 6px in six places - because .icon.aws sets overflow:hidden and the text .icon does not. It arrived with the composition and nothing here could have seen it. | sessions/2026-08-09-phase-20e-the-figures-get-an-instrument.md, evidence in sessions/2026-08-09-phase-20e-measure-instrument-break-tests.log |
| 2026-08-09 | 20g | the page fills the monitor it was written for. The cursor said the desktop, and that finding B is not the list - so the list was produced by looking: six items, four fixed here and two deliberately left to their own session. The width turned out to be the one under three of them. `main: 96rem` is ADR-0049 D1 and its reason is explicit - it is what the map's ten columns need at the legibility floor - so a FLOOR became the page's CEILING, and the page stopped at 1536px whether the monitor was 1920 or the stated primary target of 2560, which got 1024px of margin. Ten columns inside 1536px are 147px each, and `overflow-wrap: break-word` only fires when a word cannot fit AT ALL: `environme|nt` at 2560 and `RDS PostgreSQ|L` at 1440 were not typography, they were the layout reporting that the column was narrower than its text. 120rem fixed the words and the wrapped `docker ·` separator without either being touched - 1878 -> 1760px at 2560, 1878 -> 1790px at 1920 and the full width there now, 1440 unchanged because it was never at the cap. Finding B's badge grows rather than clips: `.icon.aws` never showed the overflow because it sets `overflow: hidden`, and copying that across would have hidden a WORD, so `width: fit-content` with `min-width: 1.4rem` keeps the square for a single glyph while WWW and TEST size to themselves - the AWS mark keeps its exact square, since a mark that changes size is a different mark. One fix was tried, measured and REVERTED with its figure written into the stylesheet: moving the `.p-arch` breakpoint to 1500 fixes 1440's break and costs 290px of height, 2.4 screens becoming 2.7, on a page whose whole phase is composed-not-scrolled. Every desktop overflow the instrument can report is now gone; what remains is the phone's history table, deferred far. The comb (2560: one row of ten, tallest phase 513px, 1582px of air under the other seven) and the ragged top row (573 / 279 / 235px) are measured, named and left to the session that repacks them. **ADR-0050**. No break test - nothing here is a gate; the evidence is the before/after pair from an instrument broken on purpose in 20e. $0, no cycle, nothing applied | sessions/2026-08-09-phase-20g-the-page-fills-the-monitor.md, evidence in sessions/2026-08-09-phase-20g-the-page-fills-the-monitor-measure.log |
| 2026-08-09 | 20h | the first priced cycle. The wired cost fold ran live for the first time and priced a real pair of timelines - $0.0178 .. $0.0235, lifetime 1806s, `32 created: 3 priced, 25 free, 4 not metered, 0 UNPRICED`, ADR-0046's four pairing clauses all asked on genuine data and all passed. The route was decided by two facts read out of the repository first: the fold is wired into destroy.yml ALONE (the public button's own teardown job has no pricing step), and no apply anchor existed, so a destroy dispatched on its own would have printed `nothing to price` and exited zero - green, and indistinguishable from a broken fold. Then the page, watched doing its job for the first time since the composition: a phase that finished mid-run printed yesterday's 8m 26s beside a green `done` while this run's apply took 8m 30s, because `last time` is emitted only while a phase is RUNNING - four seconds apart, asserted by nothing, `last time` appearing nowhere outside site/index.html; `ECR push` and `migrate + seed` read `not run yet` minutes after running; a verified-destroyed stage kept its icons lit while prod in the identical state stayed grey, because `.node.absent` means no record rather than not in AWS; and the cost line, an unheaded grey paragraph under the map, was read past by the person looking for it. All one root: the figures come from the terraform timeline, published when the cycle ENDS. Four first-time-live confirmations with them, and the teardown verified from the devbox against a control on a permanent level | sessions/2026-08-09-phase-20h-the-fold-prices-a-cycle-the-page-dates-it-wrong.md, the pricing step's own output in sessions/2026-08-09-phase-20h-cost-fold-live.log |
| 2026-08-09 | 20i | the page's tense. All four clauses the cursor named, all reachable on fixtures, $0. One root under three of them: every figure the map draws is published when a cycle ENDS, so during a run the page describes the cycle before it and at rest it describes a cycle rather than the present - a property of the design that the page had never been made to SAY. `last time ` existed and was hung on `since`, which is set only while a phase is RUNNING, so the one state where a stale figure is most likely to be read as this run's - finished mid-run, beside a green `done` - was the one state that never got it; the rule is now asked of the whole page, which also caught a half nobody had noticed, a phase whose turn had not come yet printing a bare figure too. `ECR push`, `migrate + seed` and the human gate said `not run yet` because Terraform reports resources and none of the three is one - permanent rather than stale, and fixed by reading `observer`, which was already on every node and had simply never been read. A verified-destroyed stage kept full colour while prod in the identical state was grey, and the fix TAKES the environments panel's verdict over the existing cycle:observed event rather than comparing timeline dates: that would have worked today and is a second definition of `destroyed` on a second host, and it would answer where the panel says `unknown`. The destroy node keeps its colour - the one node a teardown measured - and that case is a control in the fixtures, because a fix that greys everything in a destroyed environment passes every red case there is. The cost line becomes the same `.panel` as everything else on the page. **ADR-0051**, and `make page-tense-check` in ci.yml, lifting the marked block out of the BUILT page like check-live-state.mjs beside it. THE GATE'S COUPLING CHECK FAILED ITS OWN BREAK TEST TWICE: the first probe was badly aimed - one of two callers, and the check asks for at least one - and the second, with both removed, was STILL green, because the phase header's own COMMENT reads "figuresAreOlder() is that question" and the search found the name in it. A check satisfied by prose about the thing is not a check; comments are stripped now, and it was found only because a break test that failed to break was written up instead of pushed past. Ten probes with controls either side, exit statuses labelled as make's. Named rather than omitted: the gate proves what the block ANSWERS and that something asks, never that the answer reaches the pixels - what draws a node needs a DOM, is in no liftable block, and is gated by nothing  Two more findings came from the devbox, where a browser exists. contrast-check is green and needed no contract entry - `.node.gone` was written into `.node.absent`'s own selector list, so it IS that boundary, exemption and all, and the word `destroyed` carries it. And measure-page measured everything EXCEPT the thing it was run for: neither fixture set carries cost/<env>/latest.json, so the cost box keeps its `hidden` attribute and `+0px on every desktop viewport` means the box was not on the page rather than that it costs nothing - the empty result that looks clean, arriving from the fixtures this time rather than from the tool. What it did measure is a delta nobody predicted: +67/68px on the phone with every cut CLOSED and +0 at 2560, 1920 and 1440, with overflow unchanged in kind and size. The hypothesis is written down and marked unverified - the three nodes that gained a longer state line are each alone in their phase, and a desktop row takes its height from stage-apply's seven - and settling it needs a per-phase measurement rather than a per-document one, 20a's own lesson, which the packing session will be taking anyway. A third finding was the session's own: site-data-check counts the files in docs/decisions/, the chat ran it BEFORE writing ADR-0051 and never again, so it was green in the sandbox and red on the devbox with the patch applied, over one integer| sessions/2026-08-09-phase-20i-the-pages-tense.md, evidence in sessions/2026-08-09-phase-20i-page-tense-break-test.log and sessions/2026-08-09-phase-20i-page-tense-measure.log |
| 2026-08-09 | 20j | the floor the map was folded at. The cursor said 20g's packing, and the packing turned out to sit on a number nobody had measured. A broken word is now MEASURABLE: a Range over one word returns one client rect on one line and two when the line box split it, so `environme|nt` stops being something a person noticed. Asked of the page as it stood, with no hint where to look, it reported both breaks this project knows about - `RDS PostgreSQ|L` at 1440, which 20g found by eye, and `environment` on the phone, which nobody had, live in the published page since the composition landed and invisible to measure-page because a broken word is not overflow. `--node-min` has been 12rem and then 7rem, and BOTH were measured against what a node contains rather than what it has to draw, which is the question the fold asks it: derived from the words, every word whole needs 143.69px (`environment`, 89.63px) and every name unwrapped needs 295.58px, against a claim of 112px. The floor is 18.5rem - a node draws its NAME, not enough of it to be guessed at - and 143.69px is kept as the line no future number may go under rather than as the floor. The map folds to five columns in two rows at 1920 and 2560, four in three rows at 1440, one on a phone; the page grows ~300px on the desktop (1.2 screens to 1.4 on the stated primary target) and 630px on the phone, where it is not a preference since two columns were breaking a word and one is not. The one-line figure is honest only where nothing wraps - 286.92-295.58px measured on a wrapped page, 295.58 exactly after the fold - and the decision took the second reading. The top row was never three of anything (465 against 228+235+a gap is 465 against 475), so it is two columns, 4/8/8 with stretch and source order untouched, which also settles the fix 20g reverted: the five hops go from 5/12 to 8/12, 184px each at 1440, nothing breaks, and the row is 69px SHORTER instead of 290px taller - the fix was always width, and it came from packing rather than a breakpoint. In the map nothing was packed by hand: a wider column costs the tallest phase its wrapped header, 141.38 -> 98.34px, and the air falls 1432.28 -> 967.85px at 1920 and 909.65 -> 694.72 at 1440, with stretch then taking the teeth and removing no air at all. The lever for the rest - WIDE_AT 6->4, making the quality gate wide - was measured at BOTH floors and loses at both: an eleventh column of 160.81px around a node needing 143.69px, breaking `environment` at 1920 and NOT at 2560 (the 5px main gains at its cap), and at the chosen floor three rows of four costing 2039 -> 2205 at 1920 to save 2346 -> 2219 at 1440. The instrument was then aimed at its own new blind spot: stretched boxes are all exactly as tall as their row, so an air measure written the obvious way would print 0px and read as a comb that was removed - every box is measured twice now, and that is what makes the 1432 -> 968 a reading. And 20i's +67px is settled exactly, per phase at d96d9af~1 and d96d9af: three observer nodes gained a state line, +33.78px each, two of them alone in their phone row and one sharing with a phase three times its height, = +67.56px on the phone and +0 on every desktop where all three sat in the row whose height came from that same tall phase. Right in mechanism, wrong in one detail worth keeping - it named stage-apply's seven as the absorbing row, and it is the quality gate, the same phase the comb is about. **ADR-0052**. No break test: nothing here is a gate, and the broken-word measure is named as the one that could become one, together with the six breaks it finds on the phone outside the map. $0, no cycle, nothing applied | sessions/2026-08-09-phase-20j-the-floor-the-map-was-folded-at.md, evidence in sessions/2026-08-09-phase-20j-packing-measure.log |
| 2026-08-09 | 20k | the cycle the page was built for, watched. The live cycle the cursor had named since 20g and stepped over three times, run further than 20h's: stage up, promoted to prod through the approval gate, then both environments torn down and both priced. THE FINDING is that the open page never gets the figures. `readRunLayer()` reads nodes-apply.json, nodes-destroy.json, latest.json, results/<env>/latest.json and cost/<env>/latest.json, and is called ONCE in the bootstrap chain; `tick()` polls status/*.json and the GitHub runs, and the Refresh button calls `tick(true)` - so the words refresh and the figures never do. Four symptoms, one root: a green promotion left phase 6 reading `not run yet`, WORSE than the `done` it showed while running; a green prod teardown left its node `not run yet` while the published record said `measured` at 467s; prod's price could not reach the cost box; and Refresh changed only the words - `destroyed`, "these figures are from the cycle that ended" - because those come from the polled status file. Fresh words over stale figures. Left untouched for three minutes, six bucket polls at the cadence the page prints about itself, it did not converge; one hard reload fixed all four, PREDICTED before it was pressed. No fixture could have found it: measure-page mocks every remote source and never polls, and every gate here lifts a pure block and hands it data - the defect is in WHEN the page asks. Cost measured twice on the same environment and it is a lifetime: stage `$0.052648 .. $0.0581` against 20h's `$0.0178 .. $0.0235` with the identical breakdown (`32 created: 3 priced, 25 free, 4 not metered, 0 UNPRICED`), because it stood about three times as long while prod was promoted and torn down - ADR-0045 asked of two cycles rather than argued from one. prod priced for the first time ever, `$0.017217 .. $0.022735`, `34 created: 3 priced, 27 free, 4 not metered, 0 UNPRICED`. The prod approval gate witnessed live and TWICE (destroy.yml declares `environment: prod` as well), with a warning for whoever checks it next: `pending_deployments` reads empty both for "never paused" and for "already approved", and only the `approvals` endpoint tells them apart - asking the wrong one nearly produced a finding. Confirmed live on prod for the first time: 20i's four tense clauses, ADR-0043's per-step binding, 20c's `its phase is running - which node is unknown` with the other environment's node in the same phase NOT lit, and `not measured here` on the three nodes no timeline can carry. Two non-findings written down as such: a 403 on cost/stage/latest.json that was the CHAT's own fetch tool (the object was in the bucket, the devbox got 200 on all three URLs) and which cost the stage apply's observation window, and phase 8 unlit at 1m39s of a teardown, which was the map being right - its binding is two steps and neither had started. Two reader data filed beside their decisions: the legend was never opened, and at rest the brightest things on the page are the teardown and the tests while everything the cycle built is dimmed - each rule deliberate, their sum decided by nobody. Changed here: `<base target="_blank">` in the template head, one `base` rather than a `target` on each of twelve anchors, four static and eight built by string concatenation. No ADR | sessions/2026-08-09-phase-20k-the-open-page-never-gets-the-figures.md, the runs, the approval gate asked three ways, both pricing lines and the teardown reading in sessions/2026-08-09-phase-20k-cycle-evidence.log |
| 2026-08-09 | 20l | the page re-reads its figures, and the gate found a second defect. 20k's finding turned out to be four defects with one root, each invisible on the page without the other three: the run layer was never re-read, readRunLayer() ACCUMULATED so calling it twice doubled the cycle list and the unknown ledger, the re-render signature was made of states alone so an arriving price redrew nothing, and the sentence dating the map was written in the bootstrap chain. The mirror clause with them - `not reached yet` for a phase a running cycle has not got to, decided from the run's own environment list. New gate `make page-freshness-check`: freshness is not a property of a liftable block, so it drives the built page in chromium and requires an open tab to converge on what a FRESH LOAD shows - the reload that fixed all four of 20k's symptoms, turned into the property. Counting requests was rejected: a page can re-fetch and decline to draw. Eight break readings, each with the built page's hash; the gate had already gone red on the REAL defect before any were planted. One failed to break - a region the page no longer draws converges trivially - and the control now requires every compared region to move. The gate then found a defect nobody was looking for: the first observation is announced before the map has its topology and was dropped, so for 30s after a cold load a destroyed environment was drawn as standing. $0, no cycle. ADR-0053. Evidence: sessions/2026-08-09-phase-20l-freshness-break-test.log | sessions/2026-08-09-phase-20l-the-page-re-reads-its-figures.md |
| 2026-08-09 | 20m | the cycle watched through one tab, and what the map says while it runs. The live cycle the cursor had named since 20l, run in full - stage up, promoted to prod through the approval gate, both environments torn down and both priced - and watched from ONE Safari tab opened before the cycle was dispatched and never reloaded. 20l's re-read holds against real records: the front moved on its own and, specifically, prod's price reached the cost box without a reload, which is exactly what 20k could not do. THREE FINDINGS, all invisible at rest. (1) `Recent lifecycle runs` counts an in-progress run as a success: the numerator is COMPLETED runs, the denominator is all of them, so `all 7 succeeded` was printed while #32 was still running and did not change when #32 finished - it does not lag, it pre-assumes; read five times across four runs, and the clearest frame has the badge, its own right-hand half and the table row below it disagreeing in one sentence. (2) The node renderer consults the run layer BEFORE `nodeTense`, so the three nodes no timeline can ever carry - ECR push, migrate + seed, the human approval - lose their permanent `not measured here` while their phase runs, saying `which node is unknown` and then `figures publish when the cycle ends`, a promise the same cycle refutes eleven minutes later; the same caption is TRUE on the eight terraform nodes beside them, so nothing distinguishes the node whose figures are coming from the node whose figures do not exist. `which node is unknown` is false on arithmetic too: phase 5 holds one node, and in phase 8 the page had already drawn the other one as `measured`. (3) The `these figures are from the cycle that ended` disclaimer hangs on `destroyed` alone, and `unknown` is the state an environment is in whenever a run touches it - so for twelve measured minutes the map drew prod at full colour with figures and no qualification while prod was being deleted, and it repeated on stage. Predicted from the code before it could be seen; the window on the way up was missed and is recorded as missed. WHY NO GATE SAW ANY OF THEM: `page-tense-check` lifts `nodeTense` as a pure block and the function is correct - while a phase runs nothing calls it; `page-freshness-check` compares an open tab to a fresh load and both render the same wrong caption, so they converge; `site-page-check` never asks the history badge anything. Every page gate examines the page at rest or a piece of it in isolation, and no fixture here holds a cycle mid-flight with an otherwise-green history. Recorded as NOT measured rather than claimed: the convergence delay (the run went green between two observations, so `max-age=60` is bounded above by ~2 minutes and was never measured) and the map's dating sentence (the previous cycle was the same DAY, so fresh and stale are indistinguishable - as are both cost bands). `not reached yet` never appeared: it needs a node with no record at all. Cost stage $0.0529..$0.0584 (4123 s) + prod $0.0182..$0.0237 (1812 s), 0 UNPRICED both; teardown confirmed against the AWS CLI with the account line first so the five empty lines are evidence. Evidence: sessions/2026-08-09-phase-20m-cycle-evidence.log | sessions/2026-08-09-phase-20m-what-the-map-says-while-it-runs.md |
| 2026-08-10 | 21 | processes and state are two contours. A decisions session; no code, no cycle, $0. The map mixes processes (deploy, provision, destroy) with the state those processes produce, and the page already agrees with the distinction it contradicts: the panel above the map reports stage and prod as observed state while the map below draws the same environments a second time, as resource nodes nested in the phase that creates them - the arbiter between the two accounts existed only as the name of a fixture case. Nesting encodes a reference as containment, so a noun gets one verb forever, while four verbs touch an environment's database; that is exactly two of 20m's three findings (the renderer preferring the run layer, and prod.rds unreachable from the destroy phase). The third, the history badge scoring an in-flight run as a success, is a counting bug and is written down as NOT covered. ADR-0054 separates ESTATE (nouns, present tense, from observation in AWS), CYCLE (verbs, from the run layer) and ASSERTIONS (the suites, from the repository), with the binding a reference reusing ADR-0049 D6's citation mechanism. Most of the model already existed unnamed: groups[].kind, observer, perm.ecr vs build.ecr, envPanel. ADR-0039 D2b's "no separate tests panel" is overturned - its own one-story-two-places argument is what environments suffer from today, and ADR-0042 D5 had already split what a suite contains from how it ran - while its "suite x environment" half is kept, being about the run. Found free: counts.suites says 5, the map draws 4, tests/unit is invisible and nothing objects. ADR-0055 retires the lettered sub-phase: the letter was meant to mean one session and never did, the cause being that Phase 20's title had no condition of completion, so from 21 phases are integers planned with the sentence that closes them. Priced honestly: ADR-0047 D1 and ADR-0052 D2/D3 superseded, every layout figure in ADR-0050/0052 retired rather than translated, generate-topology.py's assembly and the live-state gate rewritten. Split into Phase 22 (the data, page untouched) and Phase 23 (the composition and the in-flight gate). | sessions/2026-08-10-phase-21-processes-and-state-are-two-contours.md |
| 2026-08-10 | 22 | the model in the data, and a red main nobody saw. `topology.json` to schema 3 - estate, cycle, assertions - with the page deliberately untouched; $0, no cycle. **MAIN WAS RED BEFORE THE PHASE STARTED**, and only a control run before touching anything found it: `counts.adrs` said 54, `docs/decisions` held 56, and run 31343885958 was red while `make session-close` printed `clean`. Second occurrence of 20i's 1d8980b, and the recurrence has a specific cause - session-close ran the prose check alone, and topology.json COUNTS THE FILES in docs/decisions, so a documents-only session moves a generated number without going near the generator. Fixed with a break test both ways; NOT closed, because ci.yml runs five more cheap gates that session-close does not. **THE VERB WAS NOT IN ADR-0054 AND COUNTING FOUND IT**: a phase referencing everything it touches gives the quality gate 4 own + 3 referenced = 7, which crosses WIDE_AT and takes the map from ten columns to eleven - a reflow in the phase whose plan forbids touching the page. So a reference carries a verb (creates, pushes, provisions, asserts, destroys) and a phase draws only what it `creates` (**ADR-0056**); geometry then measured identical to schema 2 - 1,7,1,4,1,8,2,2, wide=[stage-apply, prod-apply]. The obvious refusal would have been vacuous - the nodes are generated FROM the suites - so the one written asserts that every suite is either run by the cycle or declares `not_in_cycle`, and tests/unit is the second. **EIGHT GREEN GATES HID TWO BROKEN CONSUMERS**: index_members() returned ZERO addresses from the real file and the page had no DATA.phases at all, both green because every gate reads a frozen schema-2 fixture - hence a runner that probes the REAL file (0 -> 29 addresses). **AND THE GATE THE SANDBOX CANNOT RUN CAUGHT THE ONLY REGRESSION**: page-freshness-check, red 3 of 3 on the devbox, on historyTally() written into the map's WRAPPED script and called from the dashboard's - renderHistory threw on every tick, renderAll died before announceCycle and scheduleRefresh, and an open tab held the previous cycle's figures until reloaded. The coupling check now requires a lifted function to be called from the same script as its block, and the runner prints a skipped browser gate as a row rather than omitting it. Three process failures written up at length, two of them a `git checkout` over uncommitted work. Evidence: sessions/2026-08-10-phase-22-schema3-break-test.log, sessions/2026-08-10-phase-22-session-close-break-test.log | sessions/2026-08-10-phase-22-the-model-in-the-data-and-a-red-main-nobody-saw.md |
| 2026-08-11 | 23 | one list of gates, two readers. The item 22 handed forward, taken before the composition and renumbered per ADR-0055 (the composition is 24, the in-flight gate is 25). `scripts/session-close.sh` ran THREE cheap gates while `ci.yml` ran TWELVE, so `session-close: clean` and a red main were compatible states - and they had been, twice. The handover sentence said five were missing; counted from the two files it is nine, the four nobody named being publish-prefixes, cost, rates and action-pins, which sit in a different part of the workflow. The two readers also disagreed about what a gate IS - session-close invoked the scripts, ci.yml the make targets, two spellings of the same three checks. The list is now `assets/gates.json`, both readers call `make gates`, and ci.yml's twelve steps become one. THE COST OF ONE LIST IS THAT IT CAN SHRINK IN SILENCE, which two cannot: deleting a line weakens both readers at once and nothing disagrees. So `make gates-check` DISCOVERS what belongs in the list out of the repository - a `-check` name, a `scripts/check-*` recipe, or a `make <target>` line in ci.yml - and refuses on a missing gate, a target that does not exist, an exclusion with no reason, a duplicate, an empty list, and ci.yml itself missing. The middle rule earns its place: `action-pins` is a gate whose name does not say so, and the other two would both have missed it - the third only because this change removed its step. Six breaks with a control green either side, then the defect itself reproduced: an ADR planted with nothing regenerated is red in `make gates` AND in session-close, from one list. The exit numbers in the log are make's translation (2), not the script's (1), measured directly in an addendum rather than reasoned about. What it cost, deliberately: twelve named checks in the Actions UI become one, and twelve readable step names were also what made two lists look maintainable for eleven phases. Side effect worth keeping - the runner's second table names all 19 gates a plain checkout cannot run, with what each needs. **ADR-0057**. $0, no cycle, nothing applied. Main was green when the session started, checked first. A THIRD list turned up mid-session: scripts/verify-schema3.sh, written one day earlier in 22, held eight cheap gates against ci.yml's twelve - it delegates to `make gates` now and reads the browser gates from a `browser: true` flag instead of naming them. Its break test FAILED TO BREAK - mapfile over a program printing an empty line yields an array of one empty string, so the refusal saw a count of 1 and the run printed a nameless SKIP row on exit 0 - and the correction was then measured through `| tail -6`, recording tail's status. Two documented traps in ten minutes, both found by writing the break test | sessions/2026-08-11-phase-23-one-list-two-readers.md, break test in sessions/2026-08-11-phase-23-one-list-break-test.log |
| 2026-08-11 | 24 | the composition, redrawn for three contours — the model ADR-0054 decided and Phase 22 deliberately did not draw. The estate is a contour of its own (the two environments with their resource nodes, the six permanent levels out from under their cut, one caption for the whole contour); the cycle keeps the map and NAMES what each phase touches instead of containing it, so `Apply — stage` and `Apply — prod` draw no node at all and `layout.columns` falls 10 → 8, computed; the assertions contour draws all five suites, and `tests/unit` — inventoried, counted and invisible since it was written — carries its own reason for being outside a cycle. The load-bearing change is `phaseNodes()` returning a phase's own steps and resolving nothing: an estate node reached through a phase was handed a RUN-LAYER state, which is why prod.rds stood at full colour for twelve measured minutes while prod was being deleted. `check-live-state.mjs` gains claim 4 — no estate id in the run layer, checked against every case — and THE FIRST BREAK TEST DID NOT PROVE IT: reverting `phaseNodes()` fires the gate's existing both-directions comparison too, so the second break writes the seven resource nodes back into the expectation, leaving claim 4 the only thing that can speak. $0, no cycle, nothing applied. Measured on the devbox against the SAME instrument run on the previous page in the same session: 2039 → 3116px at 1920, 1.9 → 2.9 screens with the cuts closed — and 8682 → 7979 with them OPEN, so the total content is 703px SHORTER and nothing was added, the estate simply came out from under a cut. Compaction bought 78px and no more, because estate height is rows × card height and the rows come from 20j's measured 18.5rem floor; the two remaining levers are declined WITH their figures in ADR-0058 D6. The contrast refusal fired by name on a seventh state, contrast-check and page-freshness-check green | sessions/2026-08-11-phase-24-a-reference-is-a-name.md, evidence in sessions/2026-08-11-phase-24-live-state-break-test.log |
| 2026-08-11 | 25 | the gate for a cycle in flight, and an instrument that had never measured a page with figures on it. `make page-inflight-check` renders the WHOLE built page over tests/fixtures/page-inflight/ - a fixture holding a cycle mid-flight with an otherwise-green history, and carrying the RUN LAYER, the ten documents readRunLayer() fetches and no fixture here had ever supplied - and reads the sentences a visitor reads. It was RED on the page before anything was fixed, exit 1, which is a stronger entry than any reintroduced defect because 20m had watched a live cycle print it. Why nothing had seen it: nodeTense() is correct, has fourteen cases and forty-two calls in check-page-tense.mjs, and during a run the renderer consults the run layer first and never reaches it - a gate aimed at a correct function cannot see the branch that skips it, the third time here after 20a's overflow scope and 20m's converging fingerprints. Four findings closed: the badge that counted unfinished runs (fixed in 22, never demonstrable until a history was green apart from the run in flight), a node carrying observer:actions promising `figures publish when the cycle ends` when Terraform will never report it, a figure drawn during a run printed unqualified on seven stage nodes, and one NEW and found by the fixture - every prod verdict read `from the previous run` during a stage deploy that never touches prod, the exact mirror of the third, one predicate fixing both. The cursor's prediction that the gate would be the hard part was wrong: the gate took an afternoon and the FIXTURE found three things. Largest of them, ADR-0059 D5: measure-page.mjs guards requests that LEAVE the origin and the run layer is ten origin-relative paths that 404 against its own static server - ten per fixture, on BOTH sets, unmocked empty, no banner - so every layout figure argued from since 20e, ADR-0058 D6's 2039 -> 3116px included, measured the short bodies, and how much shorter is NOT MEASURED and is Phase 26. Nine break tests plus controls, tree committed before each, the built page checked to have actually changed; [7] recorded as it happened rather than as meant, because the break written for controlDiffers() was caught by the fixture audit first and [9] is the one that reaches it. Taken in the chat's sandbox on chromium-1194, which the log says in its own header; the devbox re-ran the gate green and one refusal red. $0, nothing applied. | sessions/2026-08-11-phase-25-a-figure-names-its-cycle.md, evidence in sessions/2026-08-11-phase-25-inflight-break-test.log |
| 2026-08-11 | 26 | every figure measured the short page. `measure-page.mjs` mocked three of the page's four sources and let the fourth - the run layer, ten origin-relative documents - 404 against its own static server; the page's reader turns that into `null`, so no banner, no error, every node reading `not run yet` and no figure printed at all. Fourth time here that a sound instrument was aimed at the wrong scope. It serves the layer now (REAL, captured from the live bucket - the cycle 20m ran), refuses on an origin 404, and refuses on a document that is present and unparseable, which was not in the plan and came from asking what the planned refusal does not cover. Then every figure since 20e was remeasured on its own commit with `scripts/remeasure-page-figures.sh`, the BASELINE half re-run in the same session - and it reproduced every recorded number exactly (1878, 1760/1790/2190, 2039, 8682, 3116, 7979, 1432.28, 967.85, 143.69, 295.58; 909.65 came back 909.63), which is what makes this a comparison rather than a claim. `--node-min: 18.5rem` is untouched to the hundredth - the floor is horizontal, the layer's effect is vertical. ADR-0052 D3's comb saving is 3.5x smaller at 1920 and LARGER at 1440. ADR-0058 D6 survives at 674px instead of 703px, 3.1 screens instead of 2.9. Nothing retracted. Five break entries, three refusals, controls green either side. chromium-1234 and 1194 agree to the pixel on the desktop. $0, nothing applied, the published page unchanged. ADR-0060. Evidence: sessions/2026-08-11-phase-26-layer-break-test.log, sessions/2026-08-11-phase-26-remeasurement.log | sessions/2026-08-11-phase-26-every-figure-measured-the-short-page.md |
| 2026-08-11 | 27 | the third ancestry, and a premise that was not true. `assets/contrast-contract.json` declared one probe chain and the page has THREE - ADR-0058's finding named two, and the third (the assertions contour, suite nodes straight off `div.suites` with no `.set` between) was found by walking upwards from a real node on the RENDERED page rather than reading the template, which is where two came from. `chain` -> `chains`, the gate probes each, the control is re-measured inside every one because the backdrop walk starts at the probe's parent, and the by-name refusal now names the ancestry as well as the state and the theme. The five floored states read IDENTICALLY across all three in both themes, so 24's item closes with a measurement - but the argument it was left with, "no ancestor in either chain paints a background", is false: `section.phase` paints color(srgb 1 1 1 / 0.55), and the gate prints the painters from the backdrop walk now instead of asserting the opposite. The two exempt states carry the evidence at 0.01, being the only ones drawn transparent at 45% opacity; the five with an opaque ::before are invariant to their backdrop, which is why a wrong premise produced a right answer. `gone` turned out to be a seventh state the contract had never named and its own prose had asked for in advance. Six refusals plus a real floor failure, controls either side, in a committed break script because the subject is a schema. $0, nothing applied. ADR-0061. Evidence: sessions/2026-08-11-phase-27-chains-break-test.log | sessions/2026-08-11-phase-27-the-third-ancestry-and-a-false-premise.md |
| 2026-08-11 | 28 | a cycle on a different day, five measured delays, and a qualifier that outlived its premise. The three items carried since 20m, each needing a live cycle around it, taken in ONE cycle watched through a single tab opened before the first dispatch and never reloaded, navigated or Refreshed. **`not reached yet` had to be ARRANGED** - the branch is `!record && underWay` and every node has carried a record since 2026-08-09 - so `timeline/<env>/nodes-apply.json` was backed up and deleted from the live bucket before a dispatch and restored by the cycle's own publish; the evidence is a PAIR (`not run yet` at 05:55:21 with nothing in flight, `not reached yet` at 06:01:14 with promote-prod running, held over three samples), and the plumbing was read and then RENDERED against a fixture before the bucket was touched. Production answers **403**, not the 404 the probe modelled - private bucket behind OAC without ListBucket - and `r.ok` puts both in the same branch, so the probe was right for a slightly wrong reason. **The dating sentence answered by two dates at once**: the cost box held `stage ... the cycle of 2026-08-09` beside `prod ... the cycle of 2026-08-11` in one frame, which two consecutive readings could not have settled. **Five convergence measurements instead of 20m's two-minute bound** - 1.1s, 1.6s, 8.6s where the edge held nothing, 57.6s and 61.1s where it held a copy with TTL to run - roughly uniform in [0,60]; THE TWO HIGH DRAWS ARE THE INSTRUMENT'S OWN DOING, because polling every 2 s keeps the edge warm, and that paragraph is in the script because the log without it says the opposite. The page's own `up to a minute behind the write` is now measured rather than believed. **Two findings nobody was looking for (ADR-0062)**: for about five minutes the page drew promote-prod #12's OWN figures under `these figures are from the cycle before this one`, because the premise `nothing publishes until a cycle ends` is false of a job that publishes before its last step - the mirror of 20m's third finding, same wrong predicate, and `cycle.run.id` is already in the document; and the page's first word about a run arrives on its SLOWEST clock (293 s against a 300 s ceiling, measured on destroy prod #46) because the interval is conditioned on already knowing a run is live - which lands on `Approve · a human, in the prod environment`, the one node addressed to a person, needed at the start. **Versioning declared and not applied on the dashboard bucket**, found because the plan required deleting an object: Terraform says Enabled, the account returns empty where the tfstate bucket returns `{"Status": "Enabled"}`, both rc=0. NOT diagnosed - reading the state under demo-admin returns 403 - and deliberately not chased at 22:40 with a billable cycle running. A claim made in chat on the strength of the code was wrong; the file backup was the only belt and it held. Held live, first time outside a fixture: the badge (`all 7 succeeded · 1 still going`), the per-environment suite qualifier, and the actions-observer caption. Cost **$0.0846 .. $0.0958 against a predicted $0.07 .. $0.08** - declared in advance that outside the band was a finding, and it is one: not the rates, the calendar, because two approval pauses and an observer make a cycle longer. The instrument took three patches and found five defects in itself, one of them on the devbox within an hour of delivery (`lag -0.00s`, a sighting timestamped before its own request, accepted because printf rounded -0.002 to -0.00). **METHOD FAILURE**: `gh run watch` blocked the terminal for sixteen minutes and took the only turn in which the page could be sampled, so the first `not reached yet` arrangement was published over unobserved and had to be repeated on prod. A blocking command is a choice about who gets to observe. | sessions/2026-08-11-phase-28-the-qualifier-that-outlived-its-premise.md, evidence in sessions/2026-08-11-phase-28-convergence.log, sessions/2026-08-11-phase-28-watch-convergence-self-test.log, sessions/2026-08-11-phase-28-not-reached-probe.log |
| 2026-08-12 | 29 | the fixture that publishes underneath a running page. ADR-0062's two findings live in the layer or the clock CHANGING mid-run, and every page gate here holds one moment still - which is why `page-inflight-check` was green through the whole of Phase 28 while a live page demonstrated one of them. WORSE THAN BLIND: its third claim stated the false premise in its own comment, so it REQUIRED the disclaimer on figures the run in flight had just written, and fixing the page would have reddened it. The fixture is a SEQUENCE - the page loaded once and a source changed underneath it, the bucket in one pass and the GitHub API in the other, with a sentinel on `window` required to survive so a reload cannot be mistaken for a re-read, and the clock installed rather than fixed. RED BEFORE ANYTHING WAS TOUCHED, seven of seven stage nodes, the figures moving underneath a sentence that did not: `4m 47s -> 4m 11s` under `these figures are from the cycle before this one`, and `the cycle under way` after the fix. D1 is three changes and one predicate (readRunLayer carries published_by, underWayHere becomes flightHere and answers with the RUN, nodeTense compares) - and the suite half already had it right, Phase 25 having written the same comparison two functions below. D2's interval is derived from the budget instead of from what the page already knows, floor 60 s, PAID FOR rather than borrowed: the steps of a finished run cannot change, so an idle poll costs one request instead of two and 60 an hour is one a minute - the cost ADR-0062 called unpriced is zero. THE INSTRUMENT WAS WRONG TWICE AND BOTH ARE IN THE LOG: the first D2 reading was `(105s, 120s]`, which is a fallback constant, because api.github.com is a different origin and the mock never sent access-control-expose-headers - so every reading this gate had ever taken was of the page's fallback branch, settled by the page's own advertised cadence AND by Phase 28's live ~123 s which is neither fallback; and the walk stopped at its ceiling, so the red variant reported a silence instead of a delay. It runs to 330 s now and answers `(285s, 300s]` against Phase 28's live 293 s - the fixture reproduces the finding to within one step. Checked that the stepping was not setting the answer: 120, 400 and 900 ms of settle, same window. FOUND WHILE LOADING STATE: Phase 28 closed without touching discussion-log.md or next-phases.md and session-close printed `narrative 2026-08-11, matching the newest session` - it compares the DATE, and Phase 27 closed the same day, so it was green over a block describing the wrong phase. Both documents repaired and the check now reads the phase (ADR-0063 D6). Fifteen break variants across two committed scripts, two of which found something, both in the harness and both the phase's own shape - an instrument reading part of what it is looking at. $0, no cycle, nothing applied. ADR-0063. Evidence: sessions/2026-08-12-phase-29-sequence-break-test.log, sessions/2026-08-12-phase-29-narrative-phase-break-test.log | sessions/2026-08-12-phase-29-the-fixture-that-publishes-underneath-a-running-page.md |
| 2026-09-05 | 31 | the page a stranger gets, and two documents that had no policy about their own freshness. A session driven by a DIFFERENT SUBJECT from every page phase since 20e: not *is this true and does the page say when it is true*, but *does a reader who arrives from outside and leaves in ninety seconds reach the conclusion the page is evidence for*. **MAIN WAS FOUR COMMITS BEHIND THE DEVBOX**: Phase 30's whole instrument, `scripts/watch-page.mjs`, committed 2026-08-12 and never pushed, so nothing on GitHub knew Phase 30 existed; its 11 MB of jsonl is now gitignored and the readable narrative committed like every watch log since 19e. **THE DEPENDABOT BACKLOG WAS NOT WHAT IT LOOKED LIKE**: four PRs, oldest 33 days, all red, and NOT ONE red because of its own bump - every one failed `image-scan` on the same 36 fixable HIGH findings in `util-linux`/`shadow` in the `python:3.12-slim` base, all OS packages, on a base image nobody had rebuilt since 2026-08-12, which is also the last time main ran CI at all. Verified by pulling a fresh base and running `make image-scan` locally (0 fixable, 54 unfixed, exit 0) BEFORE touching anything, then the four runs re-run on their existing head SHAs rather than rebased or overridden. #9 carried a second failure of a different kind - `local-ci` cancelled after 6h00m15s, hung on Playwright smoke; the same job on rerun took 2m13s. All four merged, zero open. Two gates caught real drift on the way back to green and one of them REFUSED rather than answered: `suite-inventory-check` declined to compare inventories collected by @playwright/test 1.62.0 and 1.62.1, because the difference would be reported as drift in the suites. **ADR-0064**: the identity bar described the system and never said what it demonstrates, so one sentence into the paragraph that was already there - no element, no section, no heading - and the Launch control, which ADR-0048 D1 correctly put in the Environments panel and called its footer, was ranking below the clocks that are the panel's own footnote by their own comment, in the same outline weight as `Refresh`. One word of D1 withdrawn; filled accent, 4.6:1 light and 8.2:1 dark, computed by hand because `contrast-check` covers map states and a control is not a state - a gap NAMED rather than assumed away. **ADR-0065, found by opening the page rather than reading anything**: a fresh tab rendered the PREVIOUS page - old header, 64 decision records - minutes after a publish whose invalidation had completed, while `curl` on the same URL in the same minute returned the new one. `publish-status.sh` has always set `no-cache, max-age=0` on the documents a CYCLE writes; `publish-site.sh` set nothing on the page itself, so index.html and topology.json went to S3 with no Cache-Control at all - not uncached but HEURISTICALLY cached, commonly a tenth of the document's age, which on a 24-day-old page buys a returning visitor's browser about two days of a copy already replaced. The finding is not the two days, it is WHICH document had no policy: the page whose whole argument is that state should be observed rather than assumed. Second load-bearing flag missing from the same `aws s3 sync` after ADR-0044's `--exclude`; the path filter gains `scripts/publish-site.sh`, a publish path that did not fire on a change to the publish path. **THE FULL CYCLE, watched through one tab** (1009 lines, 16 CHANGED frames, sentinel intact): deploy-stage green 29/29, destroy stage green with `OK: no billable stage resources remain`. Held live for the first time: the per-environment tense (stage fresh, every prod node still disclaimed), the node-level run layer, the derived poll interval walking 138s -> 126s -> 73s as the anonymous budget went, and the human-gate sentence *the page holds no credential and cannot approve anything itself*. `dated 2026-08-12` -> `dated 2026-09-05`. **ADR-0066, and the cycle found it because the session was untidy**: promote-prod #14 deployed prod, waited for the service and the HTTPS name, migrated, seeded, asserted the UI write reached RDS and smoked green - then was marked FAILED by its last line, a `git push` of the release tag refused because ADR-0065's own commit had changed a workflow file between the tested digest and the promotion. The ROLLBACK CORRECTLY DID NOT FIRE: the last-good pointer is written first of the three records for exactly this reason, and prod stayed up and healthy on the promoted digest. Two records in one step and one of them written - `put-image` runs before the push, so the release exists in ECR and not in git, not repaired retroactively. Fixed through the Git Data API, which names a commit the server already holds instead of transferring one; `workflows: write` REFUSED on purpose and the refusal written down, because a token that can rewrite the gates deciding what reaches prod is a bigger hole than an untagged release. **ADR-0044's exclusions proven live and BY ACCIDENT**: the cache fix was pushed while the cycle ran, firing `s3 sync --delete` across the bucket the cycle was writing into - the exact hazard that ADR exists for after a publish once deleted cycle 31276975666's folded results - and all five run-layer documents survived. **PROD DELIBERATELY LEFT UP** at 16:20, priced from the repository's own rates.json at ~$0.051/h ≈ $1.22/day plus ~$0.08/day of gp3, because a live app.demo.uveapp.net reads stronger than a destroyed one with a submission imminent - and the cost fold prices by LIFETIME, so this cycle's prod figure stays open until it is destroyed. Left open and named: the run badge counts unrelated workflow runs (it went DOWN from 4 to 3 as this session's own CI displaced older ones), the Launch button stays enabled during a cycle it would be refused for, and two comments now say *why* with nothing saying *whether* | sessions/2026-09-05-phase-31-the-page-a-stranger-gets.md, evidence in sessions/2026-09-05-phase-31-cycle.log, and Phase 30's own two watch logs pushed with it: sessions/2026-08-12-phase-30-at-rest.log, sessions/2026-08-12-phase-30-cycle.log |
| 2026-09-05 | 32 | an open cycle publishes a rate, not a figure. Taken in the same session as 31, because leaving prod UP made the gap impossible to look away from: the cost box read *What the last cycle cost* over the CLOSED figure of 2026-08-12 while prod was demonstrably running. `fold-cost.py` could already price an open cycle - `--destroy` has always been optional, every lifetime running to `as_of` - and nothing called it that way, because `publish-status.sh` REFUSED to publish the result. **That refusal was right about what it refused**: *it goes stale by the second, and a figure that ages silently on a public page is the claim this project keeps retracting*, and every word of it survives. What changes is WHAT AN OPEN DOCUMENT CARRIES. `component_cost()` is linear in seconds, so one number per second is the whole of a resource's pricing; an open row now publishes `usd_per_second`, the document stops being a figure that ages and becomes the INPUTS, and the page multiplies against its own clock beside the elapsed clocks that already tick there - measured $0.0409 -> $0.0418 over 60 s, exactly rate x 60, with no `render()`. The MODEL is not copied: which unit price applies to which kind stays in `assets/cost-model.json`, read only by the fold, because a second copy in JavaScript is the definition-on-two-hosts trap this project has already paid for twice. Open publishes to `latest.json` ONLY; a run-id key would litter the bucket with snapshots of one lifetime, each true for a second and none of them the record. **THE GATE WAS GREEN OVER THE NEW FIELD BEFORE IT CHECKED IT**: `check-cost.py`'s `diff()` iterates `for key in expected`, so a key the fold emits and no fixture names is INVISIBLE, and the first 11/11 was vacuous. `summarise()` carries it now and the open case's expected.json names it - derived from that fixture's own `usd / seconds`, not from the new code - and a break test bites by name: a rate emitted per minute instead of per second gives `expected 0.001, folded 0.06`, control green either side. Ninth green-over-its-own-subject here. **AND THE PAGE CRASHED ON A DOCUMENT SHAPE THIS REPOSITORY ALREADY CONTAINS**: `resources` is a LIST from `fold-cost.py` and an OBJECT keyed by address in `tests/fixtures/page-inflight/`, which is what `summarise()` produces; `.filter()` on the object is undefined, `renderCostLine()` threw, `render()` never ran - an EMPTY MAP, no visible error, and the environments panel above it rendering happily. Third renderer here to die half way down the page after 22's `historyTally()` and 20c's vanished state. `page-inflight-check` caught it and `make gates` was 12/12 GREEN over the crash, because every gate in the cheap list reads a file rather than a rendered page. The bisect was THREE WRONG GUESSES LONG - the tick, the heading, then a suspicion of flake - and what settled it was making the branch unreachable while leaving the code in place, the only variant that separates *this code is wrong* from *this code runs at all*. **It does not show on the live page yet**: the wiring runs at apply time and the prod that is up was applied before it existed. ADR-0067 | sessions/2026-09-05-phase-31-the-page-a-stranger-gets.md |
| 2026-09-05 | 33 | the public path reaches prod, and that sentence used to be the guarantee. The dashboard button ran stage and only stage; it now runs the whole lifecycle unattended - stage up, the tested digest promoted to prod, stage down, a five-minute hold with a visible clock, prod down - at the same three a day, one at a time, same lock and nonce and kill switch. **The Lambda needed NO change**: it dispatches self-service.yml by filename and that file simply does more. **WHAT WAS TRADED, SAID BEFORE WHAT WAS BUILT**: ADR-0034 had a section headed *The public path targets stage. It cannot reach prod*, and it was not a check but a structural fact - the workflow declared no prod environment, so no input value resolved a prod credential. False now. The concern was raised TWICE before any code was written and the trade taken deliberately; four documents claimed it and all four are rewritten, with ADR-0034's section KEPT and marked reversed rather than struck out because deleting the reasoning would hide what was given up. The reviewer rule is removed from the prod environment, which takes the gate off the OWNER's promotions too and not only a stranger's; the IAM half is untouched and still the stronger half, and the way back is one API call written down in security-posture.md for the reason the kill switch's delete-item is written down. **THE CYCLE CALLS, IT DOES NOT COPY**: promote-prod.yml and destroy.yml gain `workflow_call`, so ~200 lines of digest resolution, rollback pointer, ECS stability and public-HTTPS waits, prod smoke, SG revoke, orphan adoption, ALB-before-IGW ordering, CLI verification and the cost fold stay defined once - two copies would be two definitions of what *promoted* and *destroyed* mean, and this project has paid for a definition on two hosts twice. `confirm: DESTROY` stays in the callable signature rather than being defaulted away: a caller exempt from saying it is a caller that never had to mean it. **THE COUNTDOWN PUBLISHES AN INSTANT**, not a duration, and the page computes the remainder against its own clock - ADR-0067's argument one phase later, applied to a deadline; measured 4 min 11 sec -> 3 min 08 sec over 63 s, and the tile removes itself past zero rather than rendering a negative. Under `status/` deliberately, so publish-site.sh's existing --delete exclusion already covers it; a new prefix would have needed a line there and ADR-0044 exists because that line was once forgotten and a cycle's folded results were deleted from a bucket with no versioning. **TWO GATE CATCHES, BOTH SELF-INFLICTED**: the tile was first rendered from renderBanners() in the FIRST script while RUNSTATE lives in the second - a ReferenceError on every render and two red gates, the same cross-script coupling Phase 22 paid for in this same file four phases ago; and page-inflight-check refuses on ANY origin 404 (ADR-0059 D5, after ten run-layer documents 404ed in silence), so a document whose absence is its normal state is named in an explicit ONE-ITEM set - a `status/` prefix would have swallowed stage.json and prod.json, which are exactly what that rule protects. **NOT APPLIED, AND THAT IS THE OPEN RISK**: infra/self-service carries `watched_environments = [stage, prod]` for the watchdog's blunt path (it still says Environment=stage in the account) and ttl_minutes 90 -> 150 for a cycle that is now five phases summing to 150 minutes of job timeouts, with a validation refusing anything lower. Until that apply, a run that dies after the promotion leaves prod up with NOTHING able to remove it - `if: always()` is, in ADR-0035's phrase, a promise made by the thing that might not be there. terraform validate and fmt are clean, which is all a checkout can say and is not the same as a net. **AND session-close WAS GREEN OVER THIS PHASE BEING UNRECORDED**: it compares the narrative's phase against the newest session file, and with no Phase 33 file the two agreed about a picture that had simply stopped. A record that contradicts itself is caught; a record that ends is not. ADR-0068 | sessions/2026-09-05-phase-31-the-page-a-stranger-gets.md |
| 2026-09-06 | 36 | phases 34-36 in one session: what a live cycle found that no gate could. Three cycles run for real; the last is the first fully clean unattended one - six jobs green, prod up behind HTTPS and destroyed five visible minutes later, 0 RDS / 0 ALB / 0 ECS afterwards. **EVERY FINDING CAME FROM RUNNING IT OR LOOKING AT IT**, with `make gates` 12/12 green throughout. **34, ADR-0069**: a `startup_failure` strands the lock and eats a daily launch - `release-lock` is a JOB and `always()` cannot run when GitHub rejected the file before any job existed, and the watchdog reclaims ENVIRONMENTS while that run created none. Self-heals at the TTL, so the cost is a button shut for 150 minutes protecting nothing - and **ADR-0068 lengthened that by an hour without noticing**, raising the TTL for a reason that had nothing to do with it. D1 unimplemented; the manual delete-item is documented beside the kill switch, for the reason that one is. **35, ADR-0070**: the owner asked why every estate card was lit while nothing existed in AWS - 0 RDS, 0 ALB, 0 ECS at that moment - with the panel three inches above reading UNKNOWN. `envTense()` returned `unknown` for anything stale and `unknown` renders LIT; on the at-rest fixture, with both environments VERIFIED destroyed, all eight nodes came back `node measured` and zero carried `gone`. **A recorded decision said the opposite and is narrowed rather than ignored**: *the map is not entitled to a firmer answer than the panel gives* is the right principle applied backwards, because it assumed grey means *destroyed* when this page's own CSS says grey means *do not read this as live* - weaker than `unknown`, not firmer, and lit is the firmer answer that was being drawn. A cycle is a TRANSITION, so a stale reading keeps its direction: was destroyed -> not up YET -> grey; was up -> not gone YET -> lit. Only one direction is named because only one was wrong; symmetry would have dimmed a live prod the moment a teardown started. **And the old rule's second half never held**: it says a stale `up` must not colour the map, and a stale `up` renders lit - prose asserting a behaviour the code did not have, invisible to a gate that checks the WORD a function returns and never what that word is DRAWN as. **36, ADR-0071**: two consequences of ADR-0068 - *the step in flight* opened with `launch - success - 14m 22s` while destroy-prod was running, because three jobs became six and the live one is last; and a suite a public cycle deliberately never runs is drawn as *the last run here did not report this suite*, the vocabulary of a LOST report used for a design decision. `not_in_cycle` is the right vocabulary and does not reach - it is a property of a SUITE, not of a KIND of cycle. Proposed only: no page fixture can express six jobs and a subset of suites, so the fixture comes first or the fix is checked against the world that made the bug. **FOUR FIXES FROM PHASE 33 CONFIRMED LIVE**: empty `launch_id` kept the watchdog off a live stage it had deleted after four minutes the day before; `contents: write` let promote finish; `destroy-prod: always()` fired. **UNEXPLAINED AND LEFT SO**: the release tag 403ed on 09-05 and succeeded on 09-06 from the same branch, same permissions, same dispatch path. **SECOND FALSE GREEN FROM session-close IN ONE SESSION** - it compares the narrative's phase to the newest session file, and with no file for 34-36 both said 33 and agreed about a record that had stopped | sessions/2026-09-06-phases-34-36-what-a-live-cycle-found.md |
