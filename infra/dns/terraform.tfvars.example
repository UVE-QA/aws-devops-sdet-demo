# Copy to terraform.tfvars for a LOCAL apply under AWS_PROFILE=demo-admin.
# Applied once per account; a normal deploy/destroy cycle does not touch it.
owner = "UVE"
# zone_name = "demo.uveapp.net"   # default
